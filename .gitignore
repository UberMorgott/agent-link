# Node data directories and build output
/.data/
bin/
*.exe

# Third-party read-only reference checkout (see README)
/reference/

# Machine-local configs (examples/ holds the tracked templates)
*.local.json
/agentlink.json
/dist/

# Web UI dependencies; its build output internal/app/web/dist/ is committed
node_modules/
