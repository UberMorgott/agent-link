[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agent-relay-sdk"
version = "12.2.2"
description = "Python SDK for Agent Relay workflows"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
]

[project.optional-dependencies]
communicate = [
    "relaycast-sdk>=1.0.0",
    "aiohttp>=3.9",
]
# `agent_relay.node.NodeProvider` wraps the relaycast SDK's node provider client.
# aiohttp is pulled in transitively by the package's eager `agent_relay` import.
node = ["relaycast-sdk>=1.0.0", "aiohttp>=3.9"]
openai-agents = ["openai-agents>=0.1"]
claude-sdk = ["claude-agent-sdk>=0.1"]
google-adk = ["google-adk>=0.1"]
agno = ["agno>=0.1"]
crewai = ["crewai>=0.1"]
dev = [
    "relaycast-sdk>=1.0.0",
    "aiohttp>=3.9",
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-cov>=5.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/agent_relay"]

# Embedded broker binary. The publish-sdk-py CI job stages the per-platform
# binary at this path before invoking `python -m build`, then retags the wheel
# with the matching platform tag. Locally, scripts/sync-broker-dev.sh fills it
# in for editable installs.
[tool.hatch.build.targets.wheel.force-include]
"src/agent_relay/bin/agent-relay-broker" = "agent_relay/bin/agent-relay-broker"

[tool.pytest.ini_options]
pythonpath = ["src"]
asyncio_mode = "auto"
