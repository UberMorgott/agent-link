# Threads Rubric

Thread cases pass when replies retain their parent linkage, thread fetches
include parent plus replies, and reply counts reflect only child messages.
Failures indicate regressions in threaded conversation structure, alias support,
or top-level channel listing behavior.
