dependencies:
  tools:
    - type: 'mcp'
      value: 'agent-relay'
      description: 'Agent Relay real-time messaging for multi-agent coordination'
      transport: 'stdio'
      command: 'agent-relay'
      args: ['mcp']
