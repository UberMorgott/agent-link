name: Relay trusted cleanroom qualification consumer

on:
  workflow_run:
    workflows:
      - Relay cleanroom qualification request
    types:
      - completed

permissions: {}

concurrency:
  group: relay-cleanroom-consumer-${{ github.event.workflow_run.id }}-${{ github.event.workflow_run.run_attempt }}
  cancel-in-progress: false

jobs:
  verify-request:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      actions: read
      contents: read
    outputs:
      manifest_json: ${{ steps.request.outputs.manifest_json }}
      relay_sha: ${{ steps.request.outputs.relay_sha }}
      release_tag: ${{ steps.request.outputs.release_tag }}
      relay_package_run_id: ${{ steps.request.outputs.relay_package_run_id }}
      relay_package_run_attempt: ${{ steps.request.outputs.relay_package_run_attempt }}
    steps:
      - name: Check out only the trusted qualification source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          path: relay-verifier
          ref: ${{ github.workflow_sha }}
          persist-credentials: false

      - name: Set up exact Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22.22.0

      - name: Validate the completed request identity
        id: producer
        working-directory: relay-verifier
        env:
          APPROVED_ACTORS_JSON: ${{ vars.RELAY_QUALIFICATION_APPROVED_ACTORS_JSON }}
        run: |
          set -euo pipefail
          node scripts/verify-features/relay-cleanroom-qualification-request.mjs validate-event \
            --event "${GITHUB_EVENT_PATH}" \
            --approved-actors-json "${APPROVED_ACTORS_JSON}" \
            --output "${RUNNER_TEMP}/request-context.json" \
            --github-output "${GITHUB_OUTPUT}"

      - name: Select the exact bounded request artifact
        id: artifacts
        working-directory: relay-verifier
        env:
          GH_TOKEN: ${{ github.token }}
          REQUEST_RUN_ID: ${{ steps.producer.outputs.run_id }}
        run: |
          set -euo pipefail
          gh api --paginate --slurp \
            "repos/AgentWorkforce/relay/actions/runs/${REQUEST_RUN_ID}/artifacts?per_page=100" \
            > "${RUNNER_TEMP}/request-artifact-pages.json"
          node scripts/verify-features/relay-cleanroom-qualification-request.mjs select-artifact \
            --context "${RUNNER_TEMP}/request-context.json" \
            --artifact-pages "${RUNNER_TEMP}/request-artifact-pages.json" \
            --output "${RUNNER_TEMP}/request-artifact-selection.json" \
            --github-output "${GITHUB_OUTPUT}"

      - name: Download only the selected qualification request
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          artifact-ids: ${{ steps.artifacts.outputs.request_artifact_id }}
          github-token: ${{ github.token }}
          repository: AgentWorkforce/relay
          run-id: ${{ steps.producer.outputs.run_id }}
          path: ${{ runner.temp }}/relay-cleanroom-qualification-request
          merge-multiple: true

      - name: Validate and bind the exact request payload
        id: request
        working-directory: relay-verifier
        run: |
          set -euo pipefail
          chmod 600 "${RUNNER_TEMP}/relay-cleanroom-qualification-request/relay-cleanroom-qualification-request.json"
          node scripts/verify-features/relay-cleanroom-qualification-request.mjs validate-request \
            --context "${RUNNER_TEMP}/request-context.json" \
            --selection "${RUNNER_TEMP}/request-artifact-selection.json" \
            --directory "${RUNNER_TEMP}/relay-cleanroom-qualification-request" \
            --output "${RUNNER_TEMP}/trusted-qualification.json" \
            --github-output "${GITHUB_OUTPUT}"

  qualification:
    needs: verify-request
    if: ${{ needs.verify-request.result == 'success' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 360
    environment: snapshot-qualification
    permissions:
      actions: read
      contents: read
    outputs:
      # Use the reconcile step's outputs, not only the create step outputs.
      # A runner can die after Cloud accepts creation but before the create
      # step publishes its output; the deterministic idempotency key is still
      # sufficient for safe, exact recovery.
      owned_workspace_a: ${{ steps.cleanup_ids.outputs.workspace_a }}
      owned_workspace_b: ${{ steps.cleanup_ids.outputs.workspace_b }}
    env:
      CLOUD_API_URL: https://agentrelay.com/cloud
    steps:
      - name: Check out qualification verifier
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          path: relay-verifier
          ref: ${{ github.workflow_sha }}
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22.22.0

      - name: Materialize the verified immutable qualification manifest
        env:
          VERIFIED_MANIFEST_JSON: ${{ needs.verify-request.outputs.manifest_json }}
        run: |
          set -euo pipefail
          install -d -m 0700 qualification
          node -e "const fs=require('node:fs'); const value=JSON.parse(process.env.VERIFIED_MANIFEST_JSON || ''); fs.writeFileSync('qualification/relay-qualification.json', JSON.stringify(value)+'\\n', {mode:0o600,flag:'wx'})"

      - name: Validate the trusted qualification manifest again
        id: manifest
        run: |
          set -euo pipefail
          node relay-verifier/scripts/verify-features/qualification-manifest.mjs validate \
            --file qualification/relay-qualification.json \
            --output qualification/normalized.json \
            --github-output "$GITHUB_OUTPUT"

      - name: Download exact Relay, Cloud, and Relayfile Cloud qualification artifacts
        env:
          GH_TOKEN: ${{ secrets.CROSS_REPO_READ_TOKEN || github.token }}
          RELAY_PACKAGE_RUN_ID: ${{ steps.manifest.outputs.relay_package_run_id }}
          CLOUD_RUN_ID: ${{ steps.manifest.outputs.cloud_qualification_run_id }}
          CLOUD_ARTIFACT_NAME: ${{ steps.manifest.outputs.cloud_qualification_artifact_name }}
          CLOUD_ACCEPTANCE_RUN_ID: ${{ steps.manifest.outputs.cloud_acceptance_run_id }}
          CLOUD_ACCEPTANCE_ARTIFACT_NAME: ${{ steps.manifest.outputs.cloud_acceptance_artifact_name }}
          RELAYFILE_CLOUD_RUN_ID: ${{ steps.manifest.outputs.relayfile_cloud_run_id }}
          RELAYFILE_CLOUD_ARTIFACT_NAME: ${{ steps.manifest.outputs.relayfile_cloud_artifact_name }}
        run: |
          mkdir -p qualification/relay-packages/payload qualification/relay-packages/attestation qualification/cloud qualification/cloud-acceptance qualification/relayfile-cloud
          gh run download "$RELAY_PACKAGE_RUN_ID" \
            --repo AgentWorkforce/relay \
            --name relay-package-qualification \
            --dir qualification/relay-packages/payload
          gh run download "$RELAY_PACKAGE_RUN_ID" \
            --repo AgentWorkforce/relay \
            --name relay-package-qualification-attestation \
            --dir qualification/relay-packages/attestation
          gh run download "$CLOUD_RUN_ID" \
            --repo AgentWorkforce/cloud \
            --name "$CLOUD_ARTIFACT_NAME" \
            --dir qualification/cloud
          gh run download "$CLOUD_ACCEPTANCE_RUN_ID" \
            --repo AgentWorkforce/cloud \
            --name "$CLOUD_ACCEPTANCE_ARTIFACT_NAME" \
            --dir qualification/cloud-acceptance
          gh run download "$RELAYFILE_CLOUD_RUN_ID" \
            --repo AgentWorkforce/relayfile-cloud \
            --name "$RELAYFILE_CLOUD_ARTIFACT_NAME" \
            --dir qualification/relayfile-cloud

      - name: Verify source runs and GitHub artifact digests
        env:
          GH_TOKEN: ${{ secrets.CROSS_REPO_READ_TOKEN || github.token }}
          RELAY_PACKAGE_RUN_ID: ${{ steps.manifest.outputs.relay_package_run_id }}
          RELAY_PACKAGE_RUN_ATTEMPT: ${{ steps.manifest.outputs.relay_package_run_attempt }}
          RELAY_SHA: ${{ steps.manifest.outputs.relay_sha }}
          RELAY_PACKAGE_PAYLOAD_ARTIFACT_DIGEST: ${{ steps.manifest.outputs.relay_package_payload_artifact_digest }}
          RELAY_PACKAGE_ATTESTATION_ARTIFACT_DIGEST: ${{ steps.manifest.outputs.relay_package_attestation_artifact_digest }}
          CLOUD_RUN_ID: ${{ steps.manifest.outputs.cloud_qualification_run_id }}
          CLOUD_RUN_ATTEMPT: ${{ steps.manifest.outputs.cloud_qualification_run_attempt }}
          CLOUD_SHA: ${{ steps.manifest.outputs.cloud_sha }}
          CLOUD_ARTIFACT_NAME: ${{ steps.manifest.outputs.cloud_qualification_artifact_name }}
          CLOUD_ARTIFACT_DIGEST: ${{ steps.manifest.outputs.cloud_qualification_artifact_digest }}
          CLOUD_ACCEPTANCE_SOURCE_SHA: ${{ steps.manifest.outputs.cloud_acceptance_source_sha }}
          CLOUD_ACCEPTANCE_RUN_ID: ${{ steps.manifest.outputs.cloud_acceptance_run_id }}
          CLOUD_ACCEPTANCE_RUN_ATTEMPT: ${{ steps.manifest.outputs.cloud_acceptance_run_attempt }}
          CLOUD_ACCEPTANCE_ARTIFACT_NAME: ${{ steps.manifest.outputs.cloud_acceptance_artifact_name }}
          CLOUD_ACCEPTANCE_ARTIFACT_DIGEST: ${{ steps.manifest.outputs.cloud_acceptance_artifact_digest }}
          CLOUD_ACCEPTANCE_EVIDENCE_SHA256: ${{ steps.manifest.outputs.cloud_acceptance_evidence_sha256 }}
          RELAYFILE_CLOUD_RUN_ID: ${{ steps.manifest.outputs.relayfile_cloud_run_id }}
          RELAYFILE_CLOUD_RUN_ATTEMPT: ${{ steps.manifest.outputs.relayfile_cloud_run_attempt }}
          RELAYFILE_CLOUD_SHA: ${{ steps.manifest.outputs.relayfile_cloud_sha }}
          RELAYFILE_CLOUD_ARTIFACT_NAME: ${{ steps.manifest.outputs.relayfile_cloud_artifact_name }}
          RELAYFILE_CLOUD_ARTIFACT_DIGEST: ${{ steps.manifest.outputs.relayfile_cloud_artifact_digest }}
        run: |
          gh api "repos/AgentWorkforce/relay/actions/runs/$RELAY_PACKAGE_RUN_ID/attempts/$RELAY_PACKAGE_RUN_ATTEMPT" > qualification/relay-package-run.json
          gh api --paginate "repos/AgentWorkforce/relay/actions/runs/$RELAY_PACKAGE_RUN_ID/artifacts?per_page=100" --slurp > qualification/relay-package-artifacts-pages.json
          gh api "repos/AgentWorkforce/cloud/actions/runs/$CLOUD_RUN_ID/attempts/$CLOUD_RUN_ATTEMPT" > qualification/cloud-run.json
          gh api --paginate "repos/AgentWorkforce/cloud/actions/runs/$CLOUD_RUN_ID/artifacts?per_page=100" --slurp > qualification/cloud-artifacts-pages.json
          gh api "repos/AgentWorkforce/cloud/actions/runs/$CLOUD_ACCEPTANCE_RUN_ID/attempts/$CLOUD_ACCEPTANCE_RUN_ATTEMPT" > qualification/cloud-acceptance-run.json
          gh api --paginate "repos/AgentWorkforce/cloud/actions/runs/$CLOUD_ACCEPTANCE_RUN_ID/artifacts?per_page=100" --slurp > qualification/cloud-acceptance-artifacts-pages.json
          gh api "repos/AgentWorkforce/relayfile-cloud/actions/runs/$RELAYFILE_CLOUD_RUN_ID/attempts/$RELAYFILE_CLOUD_RUN_ATTEMPT" > qualification/relayfile-cloud-run.json
          gh api --paginate "repos/AgentWorkforce/relayfile-cloud/actions/runs/$RELAYFILE_CLOUD_RUN_ID/artifacts?per_page=100" --slurp > qualification/relayfile-cloud-artifacts-pages.json
          node - <<'NODE'
          const fs = require('node:fs');
          for (const [source, target] of [
            ['relay-package-artifacts-pages.json', 'relay-package-artifacts.json'],
            ['cloud-artifacts-pages.json', 'cloud-artifacts.json'],
            ['cloud-acceptance-artifacts-pages.json', 'cloud-acceptance-artifacts.json'],
            ['relayfile-cloud-artifacts-pages.json', 'relayfile-cloud-artifacts.json'],
          ]) {
            const pages = JSON.parse(fs.readFileSync(`qualification/${source}`));
            const artifacts = (Array.isArray(pages) ? pages : [pages]).flatMap((page) => page.artifacts ?? []);
            fs.writeFileSync(`qualification/${target}`, JSON.stringify({ artifacts }) + '\n', { mode: 0o600, flag: 'wx' });
          }
          const relayRun = JSON.parse(fs.readFileSync('qualification/relay-package-run.json'));
          const relayArtifacts = JSON.parse(fs.readFileSync('qualification/relay-package-artifacts.json')).artifacts;
          const relayWorkflowPath = String(relayRun.path ?? '').split('@')[0];
          const relayWorkflowRef = String(relayRun.path ?? '').split('@')[1];
          const qualificationBranch = /^(?!.*\.\.)qualification\/[A-Za-z0-9](?:[A-Za-z0-9._-]{0,126}[A-Za-z0-9])?$/;
          const expectedRelayRef = `refs/heads/${relayRun.head_branch}`;
          const safeBranchSegments = String(relayRun.head_branch ?? '').split('/').every((segment) => segment !== '.' && segment !== '..');
          if (relayRun.id !== Number(process.env.RELAY_PACKAGE_RUN_ID) || relayRun.run_attempt !== Number(process.env.RELAY_PACKAGE_RUN_ATTEMPT) || relayRun.head_sha !== process.env.RELAY_SHA || relayRun.status !== 'completed' || relayRun.conclusion !== 'success' || relayRun.name !== 'Relay package qualification' || relayWorkflowPath !== '.github/workflows/relay-package-qualification.yml' || (relayWorkflowRef !== undefined && relayWorkflowRef !== expectedRelayRef) || relayRun.event !== 'workflow_dispatch' || !qualificationBranch.test(relayRun.head_branch ?? '') || expectedRelayRef.includes('//') || !safeBranchSegments) throw new Error('Relay package producer is not the exact successful manual qualification-branch prerelease run');
          for (const [name, digest] of [
            ['relay-package-qualification', process.env.RELAY_PACKAGE_PAYLOAD_ARTIFACT_DIGEST],
            ['relay-package-qualification-attestation', process.env.RELAY_PACKAGE_ATTESTATION_ARTIFACT_DIGEST],
          ]) {
            const matches = relayArtifacts.filter((artifact) => artifact.name === name && !artifact.expired);
            if (matches.length !== 1 || matches[0].workflow_run?.id !== relayRun.id || matches[0].digest !== digest) throw new Error(`Relay ${name} artifact identity or digest mismatch`);
          }
          NODE
          node relay-verifier/scripts/verify-features/qualification-producer-artifacts.mjs cloud \
            --run qualification/cloud-run.json \
            --artifacts qualification/cloud-artifacts.json \
            --directory qualification/cloud \
            --run-id "$CLOUD_RUN_ID" \
            --run-attempt "$CLOUD_RUN_ATTEMPT" \
            --source-sha "$CLOUD_SHA" \
            --artifact-name "$CLOUD_ARTIFACT_NAME" \
            --artifact-digest "$CLOUD_ARTIFACT_DIGEST"
          node relay-verifier/scripts/verify-features/qualification-producer-artifacts.mjs cloud-acceptance \
            --run qualification/cloud-acceptance-run.json \
            --artifacts qualification/cloud-acceptance-artifacts.json \
            --directory qualification/cloud-acceptance \
            --run-id "$CLOUD_ACCEPTANCE_RUN_ID" \
            --run-attempt "$CLOUD_ACCEPTANCE_RUN_ATTEMPT" \
            --source-sha "$CLOUD_ACCEPTANCE_SOURCE_SHA" \
            --artifact-name "$CLOUD_ACCEPTANCE_ARTIFACT_NAME" \
            --artifact-digest "$CLOUD_ACCEPTANCE_ARTIFACT_DIGEST" \
            --evidence-sha256 "$CLOUD_ACCEPTANCE_EVIDENCE_SHA256" \
            --qualification-run-id "$CLOUD_RUN_ID" \
            --qualification-run-attempt "$CLOUD_RUN_ATTEMPT" \
            --qualification-artifact-digest "$CLOUD_ARTIFACT_DIGEST" \
            --snapshot-name "${{ steps.manifest.outputs.snapshot_name }}" \
            --snapshot-id "${{ steps.manifest.outputs.snapshot_id }}" \
            --relayfile-cloud-source-sha "$RELAYFILE_CLOUD_SHA" \
            --relayfile-cloud-run-id "$RELAYFILE_CLOUD_RUN_ID" \
            --relayfile-cloud-run-attempt "$RELAYFILE_CLOUD_RUN_ATTEMPT" \
            --relayfile-cloud-artifact-digest "$RELAYFILE_CLOUD_ARTIFACT_DIGEST" \
            --relayfile-cloud-deployment-id "${{ steps.manifest.outputs.relayfile_cloud_deployment_id }}" \
            --relayfile-cloud-attestation-sha256 "${{ steps.manifest.outputs.relayfile_cloud_attestation_sha256 }}"
          node relay-verifier/scripts/verify-features/qualification-producer-artifacts.mjs relayfile-cloud \
            --run qualification/relayfile-cloud-run.json \
            --artifacts qualification/relayfile-cloud-artifacts.json \
            --directory qualification/relayfile-cloud \
            --run-id "$RELAYFILE_CLOUD_RUN_ID" \
            --run-attempt "$RELAYFILE_CLOUD_RUN_ATTEMPT" \
            --source-sha "$RELAYFILE_CLOUD_SHA" \
            --artifact-name "$RELAYFILE_CLOUD_ARTIFACT_NAME" \
            --artifact-digest "$RELAYFILE_CLOUD_ARTIFACT_DIGEST"

      - name: Verify downloaded qualification bundle
        run: |
          node relay-verifier/scripts/verify-features/qualification-manifest.mjs verify-bundle \
            --file qualification/relay-qualification.json \
            --cloud-qualification qualification/cloud/qualification.json \
            --snapshot-manifest qualification/cloud/snapshot-manifest-full.json \
            --cloud-acceptance qualification/cloud-acceptance/candidate-acceptance.json \
            --relayfile-cloud-attestation qualification/relayfile-cloud/relayfile-cloud-attestation.json \
            --relay-package-payload qualification/relay-packages/payload/relay-package-attestation.json \
            --relay-package-attestation qualification/relay-packages/attestation/relay-package-qualification-attestation.json \
            --output qualification/normalized.json

      - name: Harden downloaded candidate metadata for private hydration
        run: |
          node --input-type=module <<'NODE'
          import { hardenPrivateRegularFileNoFollow } from './relay-verifier/scripts/verify-features/safe-file.mjs';
          for (const file of [
            'qualification/relay-packages/payload/candidate-install-attestation.json',
            'qualification/relay-packages/payload/candidate-package-lock.json',
          ]) {
            await hardenPrivateRegularFileNoFollow(file, { label: file });
          }
          NODE

      - name: Install exact Relay verifier and workflow source
        working-directory: relay-verifier
        run: |
          npm install --global npm@10.9.7
          npm install --global @anthropic-ai/claude-code@2.1.260 @openai/codex@0.153.3 opencode-ai@1.18.25
          claude --version | grep -F 2.1.260
          codex --version | grep -F 0.153.3
          test "$(opencode --version | tail -1)" = 1.18.25
          npm ci --ignore-scripts
          npm run build:core

      - name: Hydrate the exact producer-packed Relay candidate
        working-directory: relay-verifier
        env:
          RELAY_SHA: ${{ steps.manifest.outputs.relay_sha }}
          RELEASE_TAG: ${{ steps.manifest.outputs.release_tag }}
        run: |
          set -euo pipefail
          chmod 700 "$RUNNER_TEMP"
          version="${RELEASE_TAG#v}"
          test "v${version}" = "${RELEASE_TAG}"
          node scripts/verify-features/relay-candidate-install.mjs hydrate \
            --attestation ../qualification/relay-packages/payload/candidate-install-attestation.json \
            --tarballs ../qualification/relay-packages/payload/tarballs \
            --output "$RUNNER_TEMP/relay-candidate-install" \
            --source-sha "$RELAY_SHA" \
            --package-version "$version"
          cp "$RUNNER_TEMP/relay-candidate-install/candidate-install-attestation.json" \
            ../qualification/relay-candidate-install-attestation.json
          printf '%s' "$version" > "$RUNNER_TEMP/relay-version"

      - name: Install pinned Daytona CLI
        env:
          DAYTONA_URL: https://github.com/daytona/clients/releases/download/v0.207.1/daytona-linux-amd64
          DAYTONA_SHA256: 94dac407c1692ef537252ddccb0e129cfcaccc023fae1f08696e55d9d8a5c06b
        run: |
          curl --fail --location --silent --show-error "$DAYTONA_URL" --output "$RUNNER_TEMP/daytona"
          echo "$DAYTONA_SHA256  $RUNNER_TEMP/daytona" | sha256sum --check --strict
          chmod 0755 "$RUNNER_TEMP/daytona"
          sudo install -m 0755 "$RUNNER_TEMP/daytona" /usr/local/bin/daytona
          daytona version

      - name: Seal trusted verifier and candidate inputs before execution
        run: |
          set -euo pipefail
          mkdir -p relay-verifier/.workflow-artifacts/verify-fleet-daytona
          chmod -R a-w relay-verifier "$RUNNER_TEMP/relay-candidate-install"
          # Candidate execution may append evidence, but cannot rewrite the
          # sealed verifier sources or installed candidate tree.
          chmod -R u+w relay-verifier/.workflow-artifacts
          test ! -w relay-verifier/package.json
          test ! -w "$RUNNER_TEMP/relay-candidate-install"

      - name: Check candidate command availability (not runtime qualification)
        working-directory: relay-verifier
        env:
          VERIFY_FLEET_CLI: ${{ runner.temp }}/relay-candidate-install/install/node_modules/agent-relay/dist/cli/index.js
        run: node scripts/verify-features/qualification-capabilities.mjs --availability-only --cli "$VERIFY_FLEET_CLI"

      - name: Create isolated ephemeral Cloud workspace A
        id: workspace_a
        env:
          CLOUD_API_ACCESS_TOKEN: ${{ secrets.CLOUD_API_ACCESS_TOKEN }}
          CLOUD_API_REFRESH_TOKEN: ${{ secrets.CLOUD_API_REFRESH_TOKEN }}
          CLOUD_API_ACCESS_TOKEN_EXPIRES_AT: ${{ secrets.CLOUD_API_ACCESS_TOKEN_EXPIRES_AT }}
          CLOUD_API_REFRESH_TOKEN_EXPIRES_AT: ${{ secrets.CLOUD_API_REFRESH_TOKEN_EXPIRES_AT }}
          CLOUD_API_URL: https://agentrelay.com/cloud
          QUALIFICATION_IDEMPOTENCY_KEY: relay-qualification:${{ github.run_id }}:${{ github.run_attempt }}:a
          QUALIFICATION_WORKSPACE_NAME: relay-qualification-${{ github.run_id }}-${{ github.run_attempt }}-a
          QUALIFICATION_DEPLOYMENT_ID: ${{ steps.manifest.outputs.relayfile_cloud_deployment_id }}
          QUALIFICATION_CREDENTIAL_ROOT: ${{ runner.temp }}
          QUALIFICATION_CREDENTIAL_FILE: ${{ runner.temp }}/relay-workspace-a.json
        run: |
          chmod 0700 "$RUNNER_TEMP"
          mkdir -p qualification-cleanup
          node relay-verifier/scripts/verify-features/cleanup-qualification-workspaces.mjs --mode create > "$RUNNER_TEMP/workspace-create-a.json"
          WORKSPACE_OUTPUT="$RUNNER_TEMP/workspace-create-a.json" node -e "const fs=require('node:fs'); const p=JSON.parse(fs.readFileSync(process.env.WORKSPACE_OUTPUT,'utf8')); if(!/^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(p.workspaceId||'')) throw new Error('invalid ephemeral workspace id'); fs.appendFileSync(process.env.GITHUB_OUTPUT, 'cloud_workspace_id='+p.workspaceId+'\n')"

      - name: Create isolated ephemeral Cloud workspace B
        id: workspace_b
        env:
          CLOUD_API_ACCESS_TOKEN: ${{ secrets.CLOUD_API_ACCESS_TOKEN }}
          CLOUD_API_REFRESH_TOKEN: ${{ secrets.CLOUD_API_REFRESH_TOKEN }}
          CLOUD_API_ACCESS_TOKEN_EXPIRES_AT: ${{ secrets.CLOUD_API_ACCESS_TOKEN_EXPIRES_AT }}
          CLOUD_API_REFRESH_TOKEN_EXPIRES_AT: ${{ secrets.CLOUD_API_REFRESH_TOKEN_EXPIRES_AT }}
          CLOUD_API_URL: https://agentrelay.com/cloud
          QUALIFICATION_IDEMPOTENCY_KEY: relay-qualification:${{ github.run_id }}:${{ github.run_attempt }}:b
          QUALIFICATION_WORKSPACE_NAME: relay-qualification-${{ github.run_id }}-${{ github.run_attempt }}-b
          QUALIFICATION_DEPLOYMENT_ID: ${{ steps.manifest.outputs.relayfile_cloud_deployment_id }}
          QUALIFICATION_CREDENTIAL_ROOT: ${{ runner.temp }}
          QUALIFICATION_CREDENTIAL_FILE: ${{ runner.temp }}/relay-workspace-b.json
        run: |
          node relay-verifier/scripts/verify-features/cleanup-qualification-workspaces.mjs --mode create > "$RUNNER_TEMP/workspace-create-b.json"
          WORKSPACE_OUTPUT="$RUNNER_TEMP/workspace-create-b.json" node -e "const fs=require('node:fs'); const p=JSON.parse(fs.readFileSync(process.env.WORKSPACE_OUTPUT,'utf8')); if(!/^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(p.workspaceId||'')) throw new Error('invalid ephemeral workspace id'); fs.appendFileSync(process.env.GITHUB_OUTPUT, 'cloud_workspace_id='+p.workspaceId+'\n')"

      - name: Run exact candidate Fleet Relayflow
        id: fleet
        continue-on-error: true
        working-directory: relay-verifier
        env:
          DAYTONA_API_KEY: ${{ secrets.DAYTONA_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          VERIFY_FLEET_RELEASE_QUALIFICATION: '1'
          VERIFY_FLEET_DISPOSABLE_WORKSPACE: '1'
          VERIFY_FLEET_WORKSPACE_KEY_FILE_A: ${{ runner.temp }}/relay-workspace-a.json
          VERIFY_FLEET_WORKSPACE_KEY_FILE_B: ${{ runner.temp }}/relay-workspace-b.json
          VERIFY_FLEET_SNAPSHOT_ID: ${{ steps.manifest.outputs.snapshot_id }}
          VERIFY_FLEET_SNAPSHOT_NAME: ${{ steps.manifest.outputs.snapshot_name }}
          VERIFY_FLEET_SNAPSHOT_MANIFEST_SHA256: ${{ steps.manifest.outputs.snapshot_manifest_sha256 }}
          VERIFY_FLEET_NONCE: qualification-${{ github.run_id }}-${{ github.run_attempt }}
          VERIFY_FLEET_CLI: ${{ runner.temp }}/relay-candidate-install/install/node_modules/agent-relay/dist/cli/index.js
          VERIFY_FLEET_CANDIDATE_ATTESTATION: ${{ runner.temp }}/relay-candidate-install/candidate-install-attestation.json
          VERIFY_FLEET_MIN_CREDENTIAL_LIFETIME_SECONDS: '21600'
          AGENT_RELAY_WORKFLOW_DISABLE_RELAYCAST: '1'
          RELAYFLOWS_SANDBOX_PROVIDER: local-process
          RELAY_CLOUD_PROVISIONING_DONE: '1'
        run: |
          VERIFY_FLEET_EXPECTED_RELAY_VERSION="$(cat "$RUNNER_TEMP/relay-version")"
          export VERIFY_FLEET_EXPECTED_RELAY_VERSION
          export VERIFY_FLEET_EXPECTED_RELAY_SHA="${{ steps.manifest.outputs.relay_sha }}"
          FLEET_ATTEMPT_STARTED_AT="$(date -u +%Y-%m-%dT%H:%M:%S.%3NZ)"
          export FLEET_ATTEMPT_STARTED_AT
          echo "FLEET_ATTEMPT_STARTED_AT=$FLEET_ATTEMPT_STARTED_AT" >> "$GITHUB_ENV"
          npx relayflows run workflows/verify-fleet-daytona.ts

      - name: Reconcile exact Fleet Daytona sandboxes after candidate stop
        if: always()
        working-directory: relay-verifier
        env:
          DAYTONA_API_KEY: ${{ secrets.DAYTONA_API_KEY }}
          FLEET_ATTEMPTS: qualification-${{ github.run_id }}-${{ github.run_attempt }}-a,qualification-${{ github.run_id }}-${{ github.run_attempt }}-b
        run: |
          set -euo pipefail
          node scripts/verify-features/reconcile-fleet-daytona.mjs reconcile \
            --matrix tests/relayflows/cleanroom/fleet-daytona.matrix.json \
            --artifact-root "$RUNNER_TEMP/relay-fleet-untrusted-qualification-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}/.workflow-artifacts/verify-fleet-daytona" \
            --attempts "$FLEET_ATTEMPTS" \
            --workspace-id-a "${{ steps.workspace_a.outputs.cloud_workspace_id }}" \
            --workspace-id-b "${{ steps.workspace_b.outputs.cloud_workspace_id }}" \
            --started-at "$FLEET_ATTEMPT_STARTED_AT" \
            --output ../qualification/fleet-daytona-external-reconciliation.json

      - name: Verify candidate execution remained outside trusted inputs
        if: always()
        working-directory: relay-verifier
        run: |
          set -euo pipefail
          test ! -w package.json
          test ! -w node_modules
          test ! -w workflows/verify-fleet-daytona.ts
          test ! -w scripts/verify-features/fleet-daytona.mjs
          test ! -w packages/cli/dist/cli/index.js
          test -s "$RUNNER_TEMP/relay-candidate-install/candidate-install-attestation.json"
          test -s "$RUNNER_TEMP/relay-fleet-untrusted-qualification-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}/.workflow-artifacts/verify-fleet-daytona/qualification-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}-a/evidence.json"

      - name: Resolve exact owned workspace IDs for cleanup
        id: cleanup_ids
        if: always()
        env:
          WORKSPACE_A_OUTPUT: ${{ steps.workspace_a.outputs.cloud_workspace_id }}
          WORKSPACE_B_OUTPUT: ${{ steps.workspace_b.outputs.cloud_workspace_id }}
          CLOUD_API_ACCESS_TOKEN: ${{ secrets.CLOUD_API_ACCESS_TOKEN }}
          CLOUD_API_REFRESH_TOKEN: ${{ secrets.CLOUD_API_REFRESH_TOKEN }}
          CLOUD_API_ACCESS_TOKEN_EXPIRES_AT: ${{ secrets.CLOUD_API_ACCESS_TOKEN_EXPIRES_AT }}
          CLOUD_API_REFRESH_TOKEN_EXPIRES_AT: ${{ secrets.CLOUD_API_REFRESH_TOKEN_EXPIRES_AT }}
          RELAY_QUALIFICATION_DEPLOYMENT_ID: ${{ steps.manifest.outputs.relayfile_cloud_deployment_id }}
          CLOUD_API_URL: https://agentrelay.com/cloud
        run: |
          set -euo pipefail
          for suffix in a b; do
            idempotency="relay-qualification:${GITHUB_RUN_ID}:${GITHUB_RUN_ATTEMPT}:${suffix}"
            name="relay-qualification-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}-${suffix}"
            case "$suffix" in a) prior_workspace="$WORKSPACE_A_OUTPUT";; b) prior_workspace="$WORKSPACE_B_OUTPUT";; esac
            QUALIFICATION_OUTPUT="qualification-cleanup/reconcile-$suffix.json" \
            QUALIFICATION_IDEMPOTENCY_KEY="$idempotency" \
            QUALIFICATION_WORKSPACE_NAME="$name" \
            QUALIFICATION_DEPLOYMENT_ID="$RELAY_QUALIFICATION_DEPLOYMENT_ID" \
            QUALIFICATION_EXPECTED_WORKSPACE_ID="$prior_workspace" \
              node relay-verifier/scripts/verify-features/cleanup-qualification-workspaces.mjs --mode reconcile > /dev/null
          done
          node - <<'NODE'
          const fs = require('node:fs');
          const uuid = /^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
          for (const suffix of ['a', 'b']) {
            const prior = process.env[`WORKSPACE_${suffix.toUpperCase()}_OUTPUT`] || '';
            const value = JSON.parse(fs.readFileSync(`qualification-cleanup/reconcile-${suffix}.json`, 'utf8'));
            if (value.idempotencyKey !== `relay-qualification:${process.env.GITHUB_RUN_ID}:${process.env.GITHUB_RUN_ATTEMPT}:${suffix}` ||
                value.expectedName !== `relay-qualification-${process.env.GITHUB_RUN_ID}-${process.env.GITHUB_RUN_ATTEMPT}-${suffix}` ||
                (!value.absent && !uuid.test(value.workspaceId || '')) ||
                (value.absent && value.workspaceId !== null)) throw new Error(`invalid ${suffix} reconciliation proof`);
            if (uuid.test(prior)) {
              if (value.absent && (value.exactWorkspaceId !== prior || value.exactAbsenceStatus !== 404)) throw new Error(`reconciled ${suffix} absence was not reread by exact ID`);
              if (!value.absent && value.workspaceId !== prior) throw new Error(`reconciled ${suffix} workspace differs from the create output`);
              fs.appendFileSync(process.env.GITHUB_OUTPUT, `workspace_${suffix}=${prior}\n`);
            } else if (value.absent === true) {
              fs.appendFileSync(process.env.GITHUB_OUTPUT, `workspace_${suffix}_absent=true\n`);
            } else {
              fs.appendFileSync(process.env.GITHUB_OUTPUT, `workspace_${suffix}=${value.workspaceId}\n`);
            }
          }
          NODE

      - name: Delete exact ephemeral workspace B and verify cascade
        if: always() && steps.cleanup_ids.outputs.workspace_b != ''
        env:
          CLOUD_API_ACCESS_TOKEN: ${{ secrets.CLOUD_API_ACCESS_TOKEN }}
          CLOUD_API_REFRESH_TOKEN: ${{ secrets.CLOUD_API_REFRESH_TOKEN }}
          CLOUD_API_ACCESS_TOKEN_EXPIRES_AT: ${{ secrets.CLOUD_API_ACCESS_TOKEN_EXPIRES_AT }}
          CLOUD_API_REFRESH_TOKEN_EXPIRES_AT: ${{ secrets.CLOUD_API_REFRESH_TOKEN_EXPIRES_AT }}
          WORKSPACE_ID: ${{ steps.cleanup_ids.outputs.workspace_b }}
        run: |
          DELETE_STARTED_AT="$(date +%s)"
          QUALIFICATION_OUTPUT=qualification-cleanup/delete-b.json \
          QUALIFICATION_WORKSPACE_ID="$WORKSPACE_ID" \
            node relay-verifier/scripts/verify-features/cleanup-qualification-workspaces.mjs --mode delete > /dev/null
          DELETE_FINISHED_AT="$(date +%s)"
          DELETE_STARTED_AT="$DELETE_STARTED_AT" DELETE_FINISHED_AT="$DELETE_FINISHED_AT" WORKSPACE_ID="$WORKSPACE_ID" node - <<'NODE'
          const fs = require('node:fs');
          const started = Number(process.env.DELETE_STARTED_AT);
          const finished = Number(process.env.DELETE_FINISHED_AT);
          if (!Number.isSafeInteger(started) || !Number.isSafeInteger(finished) || finished < started) throw new Error('invalid delete timing');
          const result = JSON.parse(fs.readFileSync('qualification-cleanup/delete-b.json', 'utf8'));
          if (result.workspaceId !== process.env.WORKSPACE_ID || typeof result.operationId !== 'string' || !result.operationId) throw new Error('delete result identity is invalid');
          fs.writeFileSync('qualification/workspace-delete-b-timing.json', JSON.stringify({ workspaceId: process.env.WORKSPACE_ID, operationId: result.operationId, elapsedSeconds: finished - started }) + '\n', { mode: 0o600, flag: 'wx' });
          NODE

      - name: Delete exact ephemeral workspace A and verify cascade
        if: always() && steps.cleanup_ids.outputs.workspace_a != ''
        env:
          CLOUD_API_ACCESS_TOKEN: ${{ secrets.CLOUD_API_ACCESS_TOKEN }}
          CLOUD_API_REFRESH_TOKEN: ${{ secrets.CLOUD_API_REFRESH_TOKEN }}
          CLOUD_API_ACCESS_TOKEN_EXPIRES_AT: ${{ secrets.CLOUD_API_ACCESS_TOKEN_EXPIRES_AT }}
          CLOUD_API_REFRESH_TOKEN_EXPIRES_AT: ${{ secrets.CLOUD_API_REFRESH_TOKEN_EXPIRES_AT }}
          WORKSPACE_ID: ${{ steps.cleanup_ids.outputs.workspace_a }}
        run: |
          DELETE_STARTED_AT="$(date +%s)"
          QUALIFICATION_OUTPUT=qualification-cleanup/delete-a.json \
          QUALIFICATION_WORKSPACE_ID="$WORKSPACE_ID" \
            node relay-verifier/scripts/verify-features/cleanup-qualification-workspaces.mjs --mode delete > /dev/null
          DELETE_FINISHED_AT="$(date +%s)"
          DELETE_STARTED_AT="$DELETE_STARTED_AT" DELETE_FINISHED_AT="$DELETE_FINISHED_AT" WORKSPACE_ID="$WORKSPACE_ID" node - <<'NODE'
          const fs = require('node:fs');
          const started = Number(process.env.DELETE_STARTED_AT);
          const finished = Number(process.env.DELETE_FINISHED_AT);
          if (!Number.isSafeInteger(started) || !Number.isSafeInteger(finished) || finished < started) throw new Error('invalid delete timing');
          const result = JSON.parse(fs.readFileSync('qualification-cleanup/delete-a.json', 'utf8'));
          if (result.workspaceId !== process.env.WORKSPACE_ID || typeof result.operationId !== 'string' || !result.operationId) throw new Error('delete result identity is invalid');
          fs.writeFileSync('qualification/workspace-delete-a-timing.json', JSON.stringify({ workspaceId: process.env.WORKSPACE_ID, operationId: result.operationId, elapsedSeconds: finished - started }) + '\n', { mode: 0o600, flag: 'wx' });
          NODE

      - name: Compose and enforce runtime qualification effects
        id: runtime_effects
        if: always()
        working-directory: relay-verifier
        env:
          VERIFY_FLEET_CLI: ${{ runner.temp }}/relay-candidate-install/install/node_modules/agent-relay/dist/cli/index.js
        run: |
          node scripts/verify-features/qualification-effect-evidence.mjs \
            --manifest ../qualification/normalized.json \
            --snapshot-manifest ../qualification/cloud/snapshot-manifest-full.json \
            --cloud-acceptance ../qualification/cloud-acceptance/candidate-acceptance.json \
            --relayfile-cloud-attestation ../qualification/relayfile-cloud/relayfile-cloud-attestation.json \
            --create-a "$RUNNER_TEMP/workspace-create-a.json" \
            --credential-a "$RUNNER_TEMP/relay-workspace-a.json" \
            --create-b "$RUNNER_TEMP/workspace-create-b.json" \
            --credential-b "$RUNNER_TEMP/relay-workspace-b.json" \
            --fleet-matrix tests/relayflows/cleanroom/fleet-daytona.matrix.json \
            --fleet-artifact-root .workflow-artifacts/verify-fleet-daytona \
            --fleet-nonce "qualification-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}" \
            --delete-a ../qualification-cleanup/delete-a.json \
            --delete-a-timing ../qualification/workspace-delete-a-timing.json \
            --delete-b ../qualification-cleanup/delete-b.json \
            --delete-b-timing ../qualification/workspace-delete-b-timing.json \
            --output ../qualification/runtime-effects.json
          node scripts/verify-features/qualification-capabilities.mjs \
            --cli "$VERIFY_FLEET_CLI" \
            --effect-evidence ../qualification/runtime-effects.json

      - name: Upload candidate qualification evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: 'relay-candidate-qualification-${{ github.run_id }}-${{ github.run_attempt }}'
          path: |
            qualification/*.json
            qualification/**/*.json
            qualification/relay-packages/
            qualification/cloud/
            qualification/cloud-acceptance/
            qualification/relayfile-cloud/
            qualification/normalized.json
            qualification/cloud-acceptance/candidate-acceptance.json
            qualification/relay-candidate-install-attestation.json
            qualification/runtime-effects.json
            qualification-cleanup/delete-*.json
            relay-verifier/.workflow-artifacts/verify-fleet-daytona/
          if-no-files-found: error
          retention-days: 90

      - name: Enforce candidate qualification completion
        if: always()
        env:
          FLEET_OUTCOME: ${{ steps.fleet.outcome }}
        run: |
          node -e "if (process.env.FLEET_OUTCOME !== 'success') throw new Error('candidate Fleet qualification did not complete')"

  qualification_cleanup:
    name: Reconcile qualification workspaces
    needs: [verify-request, qualification]
    if: ${{ always() && needs.verify-request.result == 'success' && needs.qualification.result != 'skipped' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    environment: snapshot-qualification
    permissions:
      contents: read
    steps:
      - name: Check out exact cleanup CLI
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          path: relay-cleanup
          ref: ${{ github.workflow_sha }}
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22.22.0

      - name: Install only the trusted cleanup verifier
        working-directory: relay-cleanup
        run: |
          set -euo pipefail
          npm install --global npm@10.9.7
          test "$(npm --version)" = "10.9.7"
          npm ci --ignore-scripts
          npm run build:core
          mkdir -p qualification-cleanup

      - name: Reconcile exact qualification workspace IDs
        id: resolve
        working-directory: relay-cleanup
        env:
          WORKSPACE_A: ${{ needs.qualification.outputs.owned_workspace_a }}
          WORKSPACE_B: ${{ needs.qualification.outputs.owned_workspace_b }}
          RELAY_QUALIFICATION_DEPLOYMENT_ID: ${{ fromJSON(needs.verify-request.outputs.manifest_json).relayfileCloudQualification.deploymentId }}
          CLOUD_API_URL: https://agentrelay.com/cloud
          CLOUD_API_ACCESS_TOKEN: ${{ secrets.CLOUD_API_ACCESS_TOKEN }}
          CLOUD_API_REFRESH_TOKEN: ${{ secrets.CLOUD_API_REFRESH_TOKEN }}
          CLOUD_API_ACCESS_TOKEN_EXPIRES_AT: ${{ secrets.CLOUD_API_ACCESS_TOKEN_EXPIRES_AT }}
          CLOUD_API_REFRESH_TOKEN_EXPIRES_AT: ${{ secrets.CLOUD_API_REFRESH_TOKEN_EXPIRES_AT }}
        run: |
          set -euo pipefail
          for suffix in a b; do
            idempotency="relay-qualification:${GITHUB_RUN_ID}:${GITHUB_RUN_ATTEMPT}:${suffix}"
            name="relay-qualification-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}-${suffix}"
            case "$suffix" in a) prior_workspace="$WORKSPACE_A";; b) prior_workspace="$WORKSPACE_B";; esac
            QUALIFICATION_OUTPUT="qualification-cleanup/reconcile-$suffix.json" \
            QUALIFICATION_IDEMPOTENCY_KEY="$idempotency" \
            QUALIFICATION_WORKSPACE_NAME="$name" \
            QUALIFICATION_DEPLOYMENT_ID="$RELAY_QUALIFICATION_DEPLOYMENT_ID" \
            QUALIFICATION_EXPECTED_WORKSPACE_ID="$prior_workspace" \
              node scripts/verify-features/cleanup-qualification-workspaces.mjs --mode reconcile > /dev/null
          done
          node - <<'NODE'
          const fs = require('node:fs');
          const uuid = /^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
          const resolved = {};
          for (const suffix of ['a', 'b']) {
            const prior = process.env[`WORKSPACE_${suffix.toUpperCase()}`] || '';
            const value = JSON.parse(fs.readFileSync(`qualification-cleanup/reconcile-${suffix}.json`, 'utf8'));
            const expectedIdempotency = `relay-qualification:${process.env.GITHUB_RUN_ID}:${process.env.GITHUB_RUN_ATTEMPT}:${suffix}`;
            const expectedName = `relay-qualification-${process.env.GITHUB_RUN_ID}-${process.env.GITHUB_RUN_ATTEMPT}-${suffix}`;
            if (value.idempotencyKey !== expectedIdempotency || value.expectedName !== expectedName) {
              throw new Error(`invalid ${suffix} reconciliation identity`);
            }
            if (value.absent === true) {
              if (value.workspaceId !== null || value.absenceStatus !== 404) {
                throw new Error(`reconciled ${suffix} absence returned an unexpected workspace ID`);
              }
              if (uuid.test(prior)) {
                if (value.exactWorkspaceId !== prior || value.exactAbsenceStatus !== 404) {
                  throw new Error(`reconciled ${suffix} absence was not reread by exact ID`);
                }
                // The in-job cleanup may already have deleted this exact ID.
                // Preserve the raw reconcile result as normalized 404 evidence
                // and suppress a second delete; never treat it as unowned.
                resolved[suffix] = prior;
                fs.writeFileSync(
                  `qualification-cleanup/delete-${suffix}.json`,
                  `${JSON.stringify({
                    source: 'reconcile',
                    workspaceId: prior,
                    absence: { workspaceId: prior, status: value.absenceStatus },
                    reconcile: value,
                  })}\n`,
                  { mode: 0o600, flag: 'wx' }
                );
                fs.appendFileSync(process.env.GITHUB_OUTPUT, `workspace_${suffix}=${prior}\n`);
                fs.appendFileSync(process.env.GITHUB_OUTPUT, `workspace_${suffix}_already_absent=true\n`);
                continue;
              }
              fs.appendFileSync(process.env.GITHUB_OUTPUT, `workspace_${suffix}_absent=true\n`);
              continue;
            }
            if (!uuid.test(value.workspaceId || '')) throw new Error(`reconciled ${suffix} workspace has an invalid id`);
            if (uuid.test(prior) && value.workspaceId !== prior) {
              throw new Error(`reconciled ${suffix} workspace differs from the create-step output`);
            }
            resolved[suffix] = value.workspaceId;
            fs.appendFileSync(process.env.GITHUB_OUTPUT, `workspace_${suffix}=${value.workspaceId}\n`);
          }
          fs.writeFileSync(
            'qualification-cleanup/resolved.json',
            `${JSON.stringify({ version: 1, runId: process.env.GITHUB_RUN_ID, runAttempt: process.env.GITHUB_RUN_ATTEMPT, resolved }, null, 2)}\n`,
            { mode: 0o600, flag: 'wx' }
          );
          NODE

      - name: Delete exact fallback workspace B and verify cascade
        if: steps.resolve.outputs.workspace_b != '' && steps.resolve.outputs.workspace_b_already_absent != 'true'
        working-directory: relay-cleanup
        env:
          CLOUD_API_URL: https://agentrelay.com/cloud
          CLOUD_API_ACCESS_TOKEN: ${{ secrets.CLOUD_API_ACCESS_TOKEN }}
          CLOUD_API_REFRESH_TOKEN: ${{ secrets.CLOUD_API_REFRESH_TOKEN }}
          CLOUD_API_ACCESS_TOKEN_EXPIRES_AT: ${{ secrets.CLOUD_API_ACCESS_TOKEN_EXPIRES_AT }}
          CLOUD_API_REFRESH_TOKEN_EXPIRES_AT: ${{ secrets.CLOUD_API_REFRESH_TOKEN_EXPIRES_AT }}
          WORKSPACE_ID: ${{ steps.resolve.outputs.workspace_b }}
        run: |
          QUALIFICATION_OUTPUT=qualification-cleanup/delete-b.json \
          QUALIFICATION_WORKSPACE_ID="$WORKSPACE_ID" \
            node scripts/verify-features/cleanup-qualification-workspaces.mjs --mode delete > /dev/null

      - name: Delete exact fallback workspace A and verify cascade
        if: always() && steps.resolve.outputs.workspace_a != '' && steps.resolve.outputs.workspace_a_already_absent != 'true'
        working-directory: relay-cleanup
        env:
          CLOUD_API_URL: https://agentrelay.com/cloud
          CLOUD_API_ACCESS_TOKEN: ${{ secrets.CLOUD_API_ACCESS_TOKEN }}
          CLOUD_API_REFRESH_TOKEN: ${{ secrets.CLOUD_API_REFRESH_TOKEN }}
          CLOUD_API_ACCESS_TOKEN_EXPIRES_AT: ${{ secrets.CLOUD_API_ACCESS_TOKEN_EXPIRES_AT }}
          CLOUD_API_REFRESH_TOKEN_EXPIRES_AT: ${{ secrets.CLOUD_API_REFRESH_TOKEN_EXPIRES_AT }}
          WORKSPACE_ID: ${{ steps.resolve.outputs.workspace_a }}
        run: |
          QUALIFICATION_OUTPUT=qualification-cleanup/delete-a.json \
          QUALIFICATION_WORKSPACE_ID="$WORKSPACE_ID" \
            node scripts/verify-features/cleanup-qualification-workspaces.mjs --mode delete > /dev/null

      - name: Prove every create-step-owned ID has cascade absence evidence
        if: always()
        working-directory: relay-cleanup
        env:
          WORKSPACE_A: ${{ steps.resolve.outputs.workspace_a }}
          WORKSPACE_B: ${{ steps.resolve.outputs.workspace_b }}
        run: |
          node - <<'NODE'
          const fs = require('node:fs');
          const evidence = {};
          for (const suffix of ['a', 'b']) {
            const id = process.env[`WORKSPACE_${suffix.toUpperCase()}`] || '';
            if (!id) {
              evidence[suffix] = { createStepOwnedIdRecorded: false };
              continue;
            }
            const result = JSON.parse(fs.readFileSync(`qualification-cleanup/delete-${suffix}.json`, 'utf8'));
            if (result.workspaceId !== id || result.absence?.workspaceId !== id || result.absence?.status !== 404) {
              throw new Error(`workspace ${suffix} lacks exact cascade absence evidence`);
            }
            evidence[suffix] = { createStepOwnedIdRecorded: true, workspaceId: id, operationId: result.operationId, absence: result.absence };
          }
          fs.writeFileSync(
            'qualification-cleanup/absence.json',
            `${JSON.stringify({ version: 1, runId: process.env.GITHUB_RUN_ID, runAttempt: process.env.GITHUB_RUN_ATTEMPT, evidence }, null, 2)}\n`,
            { mode: 0o600, flag: 'wx' }
          );
          NODE

      - name: Upload independent cleanup evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: relay-qualification-cleanup-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            relay-cleanup/qualification-cleanup/resolved.json
            relay-cleanup/qualification-cleanup/delete-*.json
            relay-cleanup/qualification-cleanup/absence.json
          if-no-files-found: error
          retention-days: 90
