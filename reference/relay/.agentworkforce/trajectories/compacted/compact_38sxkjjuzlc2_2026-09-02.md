# Trajectory Compaction: 2026-09-02 - 2026-09-02

## Summary
A single short session (~1m of recorded window, trajectory `traj_ks4y122thqlk`) hardened the live verified-write regression test in `crates/relay-pty/src/pty.rs` for PR #1634, landing as commit `3a3232439` ("test(pty): prove blocked ConPTY cancellation") on branch `fix/claude-injection-submit-0902`. The change is test-only: it strengthens an existing live PTY regression so it deterministically reaches the state the production fix is meant to protect, rather than passing incidentally. The work sits on top of the two preceding PTY fixes in the same branch — `a7bdbd672` ("fix(pty): linearize reads with verified writes") and `4f62d28c7` ("fix(pty): prevent poll descriptor inheritance") — so this commit is the evidence layer for that read/write linearization work. The core problem was that the test's readiness signal was too weak to prove anything. A nonzero producer sequence can be produced by PTY startup control bytes alone, and even with a nonzero sequence the reader may sit between reads rather than blocked inside one. Both conditions let the test pass without ever exercising the cancellation path. The fix introduces two barriers: on all platforms, wait for the intended same-echo marker to appear in the parsed grid (this identifies the specific stale chunk the test cares about, instead of trusting a raw counter); and additionally on Windows, wait until `output_order` is held before submitting, which proves the reader is actually blocked inside ConPTY `ReadFile` and therefore that verified submission necessarily drives `CancelSynchronousIo`. Unix intentionally keeps only the parsed-grid barrier, staying platform-native rather than emulating the Windows handshake. Verification was deliberately repetition-based rather than single-run: the focused boundary regression passed 20 repetitions, the full 229-test `relay-pty` suite passed three consecutive runs, the broker suite passed 1037/1037 active, and clippy (native), clippy (Windows cross), `fmt`, `actionlint`, and diff gates were all green. The session closed clean with no unresolved findings and no follow-on work identified; the retrospective reported high confidence.

## Key Decisions (3)

| Question | Decision | Impact |
|----------|----------|--------|
| How should the live verified-write test know the PTY reader has reached the state under test before it submits? | Two-stage barrier: wait for the intended same-echo marker in the parsed grid on all platforms, and on Windows additionally wait until `output_order` is held before submitting. | The test now necessarily exercises the Windows cancellation path instead of passing incidentally; it can no longer go green while the code under test is bypassed. |
| Should Unix mirror the Windows `output_order` handshake for symmetry? | No — Unix uses only the parsed-grid barrier. | Test remains platform-native: each platform's barrier maps to the real mechanism it is proving, avoiding a false sense of parity. |
| What counts as sufficient verification for a timing-sensitive live PTY test? | Repetition-based: 20 focused reps of the boundary regression plus 3 consecutive full 229-test `relay-pty` runs, on top of broker 1037/1037, native + Windows-cross clippy, fmt, actionlint, and diff gates. | Establishes the bar for landing PTY concurrency tests in this repo — one green run is not evidence. |

## Conventions Established
- **Synchronize concurrency tests on a semantically meaningful barrier (the intended marker in the parsed grid, a held lock/order token), never on a raw counter being nonzero.**: Counters move for incidental reasons — PTY startup control bytes bump the producer sequence without the test ever reaching the state under test. (scope: `crates/relay-pty/src/pty.rs` and any live PTY/concurrency test in the relay monorepo.)
- **Prove the code path is actually entered before asserting on it — e.g. wait until the reader is demonstrably blocked in ConPTY `ReadFile` so that submission must trigger `CancelSynchronousIo`.**: Matches the repo's standing order that tests must bite: a passing assertion that never reached the guarded path is not coverage. (scope: All regression tests guarding cancellation, preemption, or race-window behavior.)
- **Keep platform-specific synchronization platform-native; don't add a Windows-shaped handshake to the Unix path for symmetry.**: The barrier should model the real underlying mechanism; a copied wait on a platform with no equivalent mechanism proves nothing. (scope: `crates/relay-pty` cross-platform tests.)
- **Gate PTY changes on the full stack: focused test ×20, full relay-pty suite ×3, broker suite, clippy native + Windows cross, fmt, actionlint, diff check.**: Windows-only code paths are cross-compiled, not run locally, so `clippy --target` for Windows is the only static guard on that half of the change. (scope: Any commit touching `crates/relay-pty/` or `crates/broker/`.)
- **Land the hardening test as its own commit (`test(pty): ...`) after the fix commits (`fix(pty): ...`), then compact the trajectory (`chore(trail): compact ...`).**: Keeps the fix and its proof separately reviewable and matches the branch's existing commit rhythm. (scope: Branch `fix/claude-injection-submit-0902`; general relay commit hygiene.)

## Lessons Learned
- A nonzero producer sequence is not evidence that a PTY reader has consumed real output — startup control bytes alone will satisfy it. (The pre-existing live verified-write test keyed off the producer sequence and could reach its assertion without ever hitting the stale-chunk scenario.) - Key readiness on the specific expected content (same-echo marker in the parsed grid), not on any monotonic counter that unrelated traffic also advances.
- "The reader has read something" and "the reader is blocked inside a read" are different states, and only the latter exercises a cancellation path. (On Windows, verified submission is supposed to break a blocked ConPTY `ReadFile` via `CancelSynchronousIo`; a reader sitting between reads never needs cancelling, so the test could pass with the cancellation code dead.) - When testing an interruption mechanism, add an explicit wait for the interruptible state (here: `output_order` held) before triggering it.
- Timing-sensitive tests need repetition counts stated as part of the result, not a single green run. (This change reported 20 focused reps and 3 full-suite reps rather than one pass.) - Report rep counts in the PR body for any concurrency regression; treat a one-run green as unverified.

## Open Questions
- Nothing was left open in the trajectory — no findings recorded, retrospective closed at high confidence. Remaining context is branch-level: whether PR #1634 (`fix/claude-injection-submit-0902`) has been reviewed/merged, and whether the `[Unreleased]` CHANGELOG heading needs a Patch entry for the underlying PTY fixes (the test commit itself is not user-visible).

## Stats
- Sessions: 1, Agents: default, Files: 1, Commits: 1
- Date range: 2026-09-02T06:04:31.047Z - 2026-09-02T06:06:19.641Z
