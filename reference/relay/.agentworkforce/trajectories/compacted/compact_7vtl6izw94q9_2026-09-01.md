# Trajectory Compaction: 2026-09-01 - 2026-09-01

## Summary
Across fourteen short lead-only sessions on branch `ci/pr-proof-broker-artifacts-0901`, PR #1632 was iterated from a naive "download a broker artifact" bridge into a fully provenance-bound, tamper-evident CI proof pipeline. The first pass (18:38) established the trust model: the broker producer runs as a `pull_request_target` workflow so its logic always comes from the trusted default branch, with `contents: read`, no persisted checkout credentials, and no secrets, while still compiling the requested PR SHA. The resolver was tightened to accept only Actions runs that are successful, exact-path, and whose `run.head_sha` equals the requested SHA, with event restricted to `pull_request_target`, a push whose `head_branch` is `main`, or a schedule whose `head_branch` is `main` (`pull_request` and `workflow_dispatch` artifacts are rejected outright); main was changed to produce an artifact for every base SHA rather than path-filtering, so an unrelated new base commit never leaves the resolver empty. Subsequent hardening added integer validation of polling bounds (NaN/Infinity/fractional/negative rejected before any GitHub request, since JS comparisons don't reject NaN and `setTimeout` normalizes non-finite delays), a same-repository head gate on the build job so fork-controlled Cargo build scripts can never run in the trusted runner, SHA-keyed concurrency with cancellation of superseded same-SHA producers, disabling the Rust cache action entirely for `pull_request_target` so PR-authored build output never enters the trusted push/schedule cache namespace, 90-day retention with rebuilds on `edited`/`ready_for_review` plus a weekly exact-main refresh, and an actionable rebase-and-push recovery message when a stale PR falls past the retention boundary. The execution-integrity thread went through three generations before landing. Generation 1 (18:53) copied verified bytes into a private mode-0500 file, rehashed the copy, opened it read-only, unlinked it, and executed via the trusted parent's `/proc/<pid>/fd` path — closing the staged-path TOCTOU. Generation 2 (19:23) found that a same-UID PR runner could still mutate a private unlinked file through `/proc`, so it moved to a Python-held `memfd` sealed with `F_SEAL_SEAL|SHRINK|GROW|WRITE|EXEC`, later extended (19:45) with explicit `MFD_EXEC` (raw `0x10` fallback) after discovering that hardened kernels with `vm.memfd_noexec=1` create legacy memfds non-executable; a disposable PID namespace at that sysctl reported seal mask `0x3f` and executed successfully. Generation 3 (19:56) superseded both: anonymous inodes break Rust's `current_exe`, which the broker needs for self-spawn. The final design executes from a stable private 0500 path protected by inherited Landlock (ABI >= 3) mutation denial — the trusted parent closes the verification fd before launch, grants mutation access only to explicit sibling writable roots, refuses `SYS_PTRACE`/`SYS_ADMIN`, and re-verifies link count, mode, size, and SHA-256 after the run, failing closed. Linux regression tests prove `current_exe` self-spawn still works while write, truncate, unlink, rename, replacement, and hard-link attacks all fail. The timing envelope was recomputed three times as facts accumulated. An initial 182-poll/30m10s bound (derived from the 30-minute producer timeout at a 10s interval, inside a 70-minute dispatcher) gave way to a 100-minute dispatcher, then to the canonical final state: 243 polls and 242 waits = 40m20s, budgeting 10 minutes of independent Actions queue/start skew (producer and dispatcher are separate workflows with independent job clocks) plus the producer's 30-minute execution deadline plus two 10-second polling intervals of slack — the exact bound is 243 rather than 242 polls because the initial lookup consumes no wait. Base and head resolve concurrently under a shared `AbortController` that cancels the sibling lookup on first failure, so the pair fits in one resolution window; the 110-minute dispatcher contains that 40m20s, the 60-minute Cloud client deadline, and 5 minutes of setup headroom, with the numeric relationship asserted in a source-contract test so future child-timeout edits cannot silently overflow the envelope. Validation at the end was green: full suite 149 files / 2275 tests, focused 64/65-test contract suites, typecheck, actionlint, Prettier, syntax and diff checks, plus direct Linux arm64 tamper validation.

## Key Decisions (8)

| Question | Decision | Impact |
|----------|----------|--------|
| How long should the resolver wait for an exact-SHA broker artifact? | 243 polls / 242 waits at a 10s interval = 40m20s, superseding the earlier 182-poll/30m10s bound | Cold builds and queued producers no longer fail a valid resolution; the bound is asserted numerically in a source-contract test. |
| How to execute the verified broker so a same-UID PR runner cannot tamper with it? | A stable private 0500 path executed under inherited Landlock ABI >= 3 mutation denial, superseding both the anonymous unlinked inode and the exec-sealed memfd | Broker self-spawn works in proofs; content and path mutation are denied, with post-run link-count/mode/size/SHA-256 verification failing closed. |
| How does the resolver bind an artifact to the requested source SHA? | Validate `run.head_sha` against the request, plus exact path, success status, and trusted event (`pull_request_target`, or `push`/`schedule` with `head_branch` equal to `main`); main produces every base SHA | Spoofed or mismatched artifacts are rejected; `pull_request` and `workflow_dispatch` artifacts are never accepted. |
| Should fork PRs be compiled by the trusted producer? | Gate the build job so pull_request_target requires head repo == github.repository; push and schedule stay enabled | Fork-controlled code cannot execute in the trusted builder while same-repo PR artifact production is preserved. |
| Should PR-authored broker builds share the Rust cache? | Historical intermediate: skip caching only for `pull_request_target`; superseded by `compact_yk93pl8j0xrp`, which removes caching from every producer event | The current producer always builds cold and isolates PR-authored Cargo under a dedicated UID; the 40m20s resolver bound covers the cold build. |
| What happens when a PR's exact broker artifact has expired? | Emit an actionable update/rebase-and-push recovery and document the bounded policy in the case-author guide | Stale PRs get a clear remediation path instead of an opaque resolution failure. |
| How to bound the outer dispatcher job? | 110 minutes, enforced by a source-contract test asserting resolution + Cloud execution + setup headroom fits | Child-timeout changes cannot silently exceed the job envelope; grew from 70 → 100 → 110 minutes as inner bounds were corrected. |
| Should duplicate same-SHA producers be allowed to run? | SHA-keyed concurrency group with cancel-in-progress | Deterministic, resource-bounded artifact production. |

## Conventions Established
- **Encode timing relationships as source-contract tests, not comments — assert that poll count × interval, producer timeout, Cloud deadline, and setup headroom fit within the declared job timeout**: Timeouts are split across separate workflow files and drift silently; a numeric assertion makes overflow a test failure. (scope: All RelayFlow proof CI workflows and their resolver code)
- **Validate resolver configuration as bounded integers before issuing any network request**: JS comparisons do not reject NaN and setTimeout normalizes non-finite delays, so an invalid interval degrades into a tight loop against the GitHub API. (scope: Broker artifact resolver polling parameters)
- **Fail closed on integrity: re-verify link count, mode, size, and SHA-256 after execution, and treat any mismatch as proof failure**: Pre-execution digest checks are TOCTOU-vulnerable when a same-UID PR runner shares the filesystem. (scope: Trusted broker execution bridge on Linux)
- **Trusted-parent/untrusted-child split: the parent verifies, closes its verification fd, restricts the sandbox (Landlock roots, no SYS_PTRACE/SYS_ADMIN), then launches**: Keeps the verification handle out of the untrusted process while granting only explicit sibling writable roots. (scope: Broker proof execution)
- **Document the proof boundary explicitly: immutable exact-SHA bytes are machine-guaranteed; PR-authored argv, environment, and semantic assertions remain reviewer-trusted**: Prevents over-claiming what the hardening proves during review. (scope: PR #1632 reviewer documentation and case-author guide)
- **Run the full test suite sequentially, never in parallel with typecheck**: Typecheck rebuilds package dist directories and races the test run; the same 2275-test suite passed cleanly when rerun sequentially. (scope: Local validation in this monorepo)

## Lessons Learned
- Hardening that preserves bytes can still break runtime behavior — memfd kept the verified image intact but destroyed Rust's `current_exe`, which the broker needs to self-spawn (Two full generations (anonymous inode, then exec-sealed memfd) were built and validated before the self-spawn regression forced the Landlock redesign.) - When sandboxing an executable, enumerate the runtime introspection it depends on (`current_exe`, `/proc/self/exe`, argv[0] re-exec) before choosing an execution vector.
- Kernel hardening policy silently changes syscall defaults — `vm.memfd_noexec=1` makes legacy memfds non-executable (Discovered while validating memfd portability; fixed by passing MFD_EXEC (raw 0x10 fallback) and requiring F_SEAL_EXEC, verified in a disposable PID namespace reporting seal mask 0x3f.) - Validate low-level syscall assumptions under hardened sysctls in a throwaway namespace, not just on the dev host.
- Independent workflows have independent job clocks — deriving a wait purely from the producer's own timeout under-budgets by the entire queue/start delay (The 182-poll bound derived from the 30-minute producer timeout could still fail because the producer's clock starts after the resolver's.) - Always add an explicit queue/start-skew allowance plus interval slack when one workflow polls for another's output.
- pull_request_target trusts the workflow file, not the code it checks out (Two separate fixes were needed — gating the build to same-repo heads, and disabling the Rust cache — because PR Cargo build scripts execute in that trusted context.) - Audit every trusted-context side effect (checkout, build scripts, caches, artifact namespaces) whenever a workflow uses pull_request_target.
- Private and unlinked is not immutable when the attacker shares the UID (The mode-0500 unlinked file from generation 1 was still mutable through /proc by the same-UID runner.) - Reason about /proc reachability and same-UID access explicitly; prefer kernel-enforced denial (seals or Landlock) over filesystem permissions alone.
- Retention boundaries are part of the trust model, not an operational detail (The producer intentionally refuses arbitrary historical SHAs, so 90-day artifact expiry becomes a hard failure for stale PRs.) - Pair every fail-closed provenance rule with a documented, actionable recovery path in the user-facing guide.

## Open Questions
- The proof boundary is explicitly limited to immutable exact-SHA broker bytes — PR-authored argv, environment, and semantic assertions remain reviewer-trusted, so a malicious case can still shape what the trusted binary is asked to do.
- The Landlock bridge requires ABI >= 3; behavior on older kernels or non-Linux runners (and whether the proof degrades or hard-fails there) is not recorded in these trajectories.
- No files-changed or commit list is attached to any trajectory, so the concrete resolver/workflow file paths behind the 243-poll bound and the Landlock launcher must be recovered from the five branch commits (a8e4df123 back to 067f2bf29).
- Cold PR broker builds now run cache-less by design; whether the 30-minute producer timeout plus 10-minute skew allowance holds for a genuinely cold Rust build is unvalidated against real cold-build wall-clock data.
- The weekly scheduled main refresh is accepted only when the run reports head_branch main — the behavior for repositories with long push gaps between weekly refreshes has not been exercised end to end.

## Stats
- Sessions: 14, Agents: default, Files: 0, Commits: 0
- Date range: 2026-09-01T18:38:22.553Z - 2026-09-01T20:15:29.935Z
