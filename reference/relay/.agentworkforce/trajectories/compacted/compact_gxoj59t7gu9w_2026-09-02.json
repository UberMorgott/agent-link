{
  "id": "compact_gxoj59t7gu9w",
  "version": 1,
  "type": "compacted",
  "compactedAt": "2026-09-02T05:10:27.751Z",
  "sourceTrajectories": [
    "traj_3cdd038ry6vj"
  ],
  "dateRange": {
    "start": "2026-09-02T05:00:07.771Z",
    "end": "2026-09-02T05:10:14.986Z"
  },
  "summary": {
    "totalDecisions": 1,
    "totalEvents": 2,
    "uniqueAgents": [
      "default"
    ]
  },
  "decisionGroups": [
    {
      "category": "other",
      "decisions": [
        {
          "question": "Use producer-tagged PTY output plus atomic write-boundary capture",
          "chosen": "Use producer-tagged PTY output plus atomic write-boundary capture",
          "reasoning": "Consumer byte offsets cannot distinguish an earlier chunk still queued behind tokio select. A relay-pty sequence assigned at read time and linearized with verified write queue admission closes the race for both fleet PTY and wrap paths while preserving echo-before-ack handling.",
          "fromTrajectory": "traj_3cdd038ry6vj"
        }
      ]
    }
  ],
  "keyLearnings": [],
  "keyFindings": [],
  "filesAffected": [
    "crates/broker/src/broker/delivery_verification.rs",
    "crates/broker/src/pty_worker.rs",
    "crates/broker/src/wrap.rs",
    "crates/relay-pty/src/pty.rs"
  ],
  "commits": [
    "99d4836a6"
  ],
  "narrative": "A single ~10-minute session on branch `fix/claude-injection-submit-0902` closed the last open review thread on PR #1634: echo verification in the Rust broker could confirm a delivery against a *stale* PTY chunk. The prior design (from earlier commits in the same chain — ae164c1ae `refactor(broker): share delivery verification policy`, 31519a156 `fix(broker): preserve activity before write ack`) captured an absolute consumer-side byte offset, `VerificationOutput::boundary() -> usize`, immediately before submitting Body+Enter. That boundary is only meaningful for output the consumer has already appended; a chunk that the PTY reader thread had already read but that was still sitting in the bounded `mpsc` queue behind a `tokio::select!` arm would be appended *after* the boundary was sampled and could then satisfy an identical earlier delivery's expected echo — a real false-positive path whenever the same message body is delivered twice. The fix (commit 99d4836a6, `fix(broker): reject queued stale PTY echoes`, 335+/78- across 4 files) moves the identity of a PTY read from the consumer to the producer. `crates/relay-pty/src/pty.rs` introduces `pub struct PtyOutput { bytes, sequence }` (with `as_bytes()`, `sequence()`, `Deref<Target=[u8]>`, `AsRef<[u8]>`), and `PtySession::spawn` now yields `mpsc::Receiver<PtyOutput>` instead of `Receiver<Vec<u8>>`. The reader thread assigns the sequence via `output_sequence.fetch_add` *before* grid parsing or `blocking_send`, under an `output_order: Arc<Mutex<()>>`. That same mutex is taken by the new `enqueue_write_with_output_boundary`, which atomically samples the producer watermark and admits the write to the FIFO queue, exposed as `submit_write_paced_with_output_boundary` and `submit_write_paced_with_followup_and_output_boundary` (both return `(oneshot::Receiver<io::Result<()>>, u64)`). Linearizing sequence assignment with queue admission is what actually closes the race — sampling an atomic alone would still allow a read to slot in between the load and the enqueue. On the consumer side, `crates/broker/src/broker/delivery_verification.rs` rewrote `VerificationOutput` to retain **raw bytes** (`Vec<u8>`) indexed by a `VecDeque<VerificationSegment>` of `{sequence, start_offset, end_offset}`; `PendingVerification::output_boundary` changed `usize -> u64`, `since(boundary: u64) -> Cow<'_, str>` finds the first segment with `sequence > boundary` and lossily decodes from there, and `retained()` also returns `Cow`. Retaining raw bytes removes the previous `floor_char_boundary` dance and guarantees a split UTF-8 sequence cannot migrate bytes across a segment boundary. Both injection paths — `pty_worker.rs` (fleet PTY) and `wrap.rs` (interactive wrap) — now call `echo_buffer.push_output(chunk.sequence(), chunk.as_bytes())` as the *first* statement in their `pty_rx.recv()` select arm, before any decode/passthrough/async work, and the old `push_str(&text)` accumulation (including the `utf8_decoder.flush()` tail append at exit) was deleted. Three deterministic regressions were added: `verification_ignores_matching_output_queued_before_write_submission`, the updated `echo_arriving_before_worker_write_ack_is_confirmed_immediately` (now driving sequences 1/2/3 explicitly), and a live-PTY test `verified_write_boundary_includes_output_still_queued_for_consumer` that deliberately does not drain `rx` so an identical `same-echo` chunk is provably queued at submission time. Full relay-pty + broker test suites and clippy reported green; the agent self-rated the work high-confidence.",
  "decisions": [
    {
      "question": "How should echo verification distinguish a delivery's own echo from an identical earlier one?",
      "chosen": "Producer-assigned PTY read sequences (`PtyOutput.sequence`) plus an atomic write-boundary capture linearized with queue admission, replacing consumer-side absolute byte offsets.",
      "reasoning": "A consumer byte offset only reflects output the event loop has already appended. A chunk read earlier but still queued behind `tokio::select!` gets appended after the boundary is sampled, so an identical earlier delivery's echo looks fresh. Tagging at read time in the reader thread — before grid parsing, locks, or the bounded-channel wait — gives every chunk an identity that predates any consumer scheduling.",
      "impact": "`PendingVerification.output_boundary` is now `u64` (a sequence, not an offset) across `delivery_verification.rs`, `pty_worker.rs`, and `wrap.rs`; `PtySession::spawn` returns `Receiver<PtyOutput>`, a breaking change for any relay-pty consumer."
    },
    {
      "question": "Is sampling the atomic output watermark before enqueueing the write sufficient?",
      "chosen": "No — guard both sequence assignment (reader thread) and watermark-sample + `enqueue_user_write` (submitter) with the shared `output_order: Arc<Mutex<()>>`.",
      "reasoning": "Without the shared lock, the reader can assign a new sequence in the window between the submitter's `output_sequence.load` and the write actually being admitted to the FIFO queue, re-opening the exact race the sequence was meant to close.",
      "impact": "New private `PtySession::enqueue_write_with_output_boundary` is the single choke point; the two public `*_with_output_boundary` submit methods both route through it and return `(ack_rx, boundary)` as one unit so callers cannot sample and enqueue separately."
    },
    {
      "question": "Should the verification tail keep storing decoded `String` text?",
      "chosen": "Retain raw `Vec<u8>` with a `VecDeque<VerificationSegment>` index; decode lazily via `String::from_utf8_lossy` and return `Cow<'_, str>`.",
      "reasoning": "Decoding per-chunk lets a split multi-byte UTF-8 sequence be completed by a later chunk, which moves bytes across a sequence boundary and corrupts the very partition verification depends on. Raw retention also drops the `util::ansi::floor_char_boundary` trimming special case.",
      "impact": "`since()` / `retained()` return `Cow`, so callers use `&observed` or `.into_owned()`; trimming advances `base_offset` and pops segments whose `end_offset <= base_offset`, keeping sequences monotonic across trims."
    },
    {
      "question": "Fix only the fleet PTY path, or both injection paths?",
      "chosen": "Apply the same contract in `pty_worker.rs` and `wrap.rs` simultaneously.",
      "reasoning": "Both consume `pty_rx` in a `select!` and both submit verified injections; fixing one would leave the identical race live in the other and split the shared `delivery_verification` policy that ae164c1ae had just unified.",
      "impact": "The two paths stay behaviorally identical, and the shared `VerificationOutput` API has exactly one semantics for both."
    }
  ],
  "conventions": [
    {
      "pattern": "Push raw PTY bytes into `echo_buffer` via `push_output(chunk.sequence(), chunk.as_bytes())` as the very first statement of the `pty_rx.recv()` select arm — before ANSI decode, terminal passthrough, trust-prompt handling, or any `.await`.",
      "rationale": "Any work done before recording the chunk reintroduces consumer-side scheduling into a boundary that is supposed to be producer-defined.",
      "scope": "`crates/broker/src/pty_worker.rs`, `crates/broker/src/wrap.rs`, and any future PTY consumer."
    },
    {
      "pattern": "Never sample an ordering watermark and act on it in two steps; return the watermark from the same call that performs the ordered action (`submit_write_paced_with_output_boundary` returns `(ack_rx, boundary)` under one lock).",
      "rationale": "Split sample/act APIs make the race re-creatable at every call site; a fused API makes it unrepresentable.",
      "scope": "`crates/relay-pty/src/pty.rs` write-submission API."
    },
    {
      "pattern": "Keep byte buffers raw and defer UTF-8 decoding to the read/match site when byte positions carry semantic meaning; expose `Cow<'_, str>` rather than forcing an allocation.",
      "rationale": "Incremental decoding relocates bytes across boundaries; lazy lossy decoding preserves the partition and avoids allocating on the hot verification path.",
      "scope": "`crates/broker/src/broker/delivery_verification.rs::VerificationOutput`."
    },
    {
      "pattern": "Assert invariants that the type system cannot express with `debug_assert!` at the mutation point — e.g. `debug_assert!(sequence > self.last_sequence, \"PTY output sequences must be strictly monotonic\")` in `push_output`.",
      "rationale": "Catches a mis-ordered producer in debug/test builds without adding release-path cost.",
      "scope": "Rust broker crates."
    },
    {
      "pattern": "Write regressions that force the racy interleaving deterministically rather than relying on timing: set `output_boundary = 2` while the consumer has only appended sequence 1, then `push_output(2, ...)` and assert it stays stale; in the live-PTY test, deliberately do not drain `rx` and spin on `output_sequence` until the reader has tagged the chunk.",
      "rationale": "A race fix that cannot be shown failing before the patch is not evidence; hand-driving the sequence numbers makes the failure reproducible on every run.",
      "scope": "`#[cfg(test)] mod tests` in `delivery_verification.rs`, `pty_worker.rs`, and `crates/relay-pty/src/pty.rs`."
    },
    {
      "pattern": "Document the *why* of an ordering constraint in the doc comment on the field/struct that carries it (see the rewritten comments on `PendingVerification.output_boundary`, `PtySession.output_order`, and `PtyOutput`).",
      "rationale": "The correctness argument lives in scheduling behavior that is invisible from the code shape; the next reader will otherwise \"simplify\" the lock away.",
      "scope": "Rust broker/relay-pty."
    }
  ],
  "lessons": [
    {
      "lesson": "A boundary is only as trustworthy as the point at which it is observed — consumer-side positions cannot describe producer-side ordering.",
      "context": "The pre-existing `output_boundary: usize` (a `VerificationOutput` byte offset) looked correct and was even documented as guarding against \"an identical earlier delivery in the retained output tail,\" but it was blind to chunks already read and queued in the async channel.",
      "recommendation": "When reasoning about ordering across a channel, ask where identity is assigned. If the answer is \"after the queue,\" the invariant is about the consumer's schedule, not about the event."
    },
    {
      "lesson": "An atomic counter is not a substitute for linearization when two operations must be ordered relative to each other.",
      "context": "`output_sequence.load(Acquire)` followed by `enqueue_user_write` leaves a window in which the reader can bump the sequence; the `output_order` mutex, held on both sides, is what makes the pair atomic.",
      "recommendation": "Whenever a sampled counter is used to classify events from another thread, hold the same lock the counter's producer holds — or accept the window explicitly and prove it is harmless."
    },
    {
      "lesson": "Incremental UTF-8 decoding silently invalidates byte-indexed invariants.",
      "context": "The old code appended decoded `text` and had to `floor_char_boundary` on trim; a chunk-splitting multi-byte sequence would have been completed by the next chunk, moving bytes past the boundary.",
      "recommendation": "Index over raw bytes and decode lossily at the point of comparison; the extra `Cow` is cheaper than an unprovable boundary."
    },
    {
      "lesson": "Fixing shared policy in one of two consumers re-forks a codebase that was just unified.",
      "context": "Commit ae164c1ae had deliberately shared delivery-verification policy between `pty_worker.rs` and `wrap.rs`; the sequence change had to land in both select arms in the same commit.",
      "recommendation": "After extracting shared policy, treat every downstream consumer of that policy as part of the blast radius of any change to its contract."
    },
    {
      "lesson": "Changing an ordering primitive is a breaking API change even when the diff looks internal.",
      "context": "`PtySession::spawn` went from `Receiver<Vec<u8>>` to `Receiver<PtyOutput>`; `Deref<Target=[u8]>` and `AsRef<[u8]>` were added specifically so existing byte-slice call sites (e.g. `stdout.write_all(&chunk)`) kept compiling.",
      "recommendation": "When widening a channel payload into a struct, add the deref/as-ref impls up front to keep the change surgical, but still audit every consumer — the useful behavior change is exactly the one the impls hide."
    }
  ],
  "openQuestions": [
    "Is `VERIFICATION_OUTPUT_KEEP_BYTES` trimming still safe now that a segment can be partially trimmed? `since()` clamps with `segment.start_offset.max(self.base_offset)`, so a boundary whose segment was partly drained silently widens the matched window — no test covers trim-plus-boundary interaction beyond the existing monotonic-offset test.",
    "The `utf8_decoder.flush()` tail on child exit no longer feeds `echo_buffer` (it only reaches `stream_buffer`). Confirm the `agent_exit` payload's trailing 2000 bytes are unchanged in practice, since final incomplete bytes are now surfaced only through the raw retained buffer's lossy decode.",
    "Are there other consumers of `PtySession::spawn` outside `pty_worker.rs` and `wrap.rs` (e.g. SDK/test harnesses, other crates) that need updating for the `PtyOutput` payload change?",
    "PR #1634's review status after this commit was not recorded — whether the reviewer accepted the producer-sequence approach, and whether the branch has been pushed/merged, is unresolved in this trajectory.",
    "No performance check was recorded for the added `output_order` mutex on the PTY reader hot path (taken once per 4KB read) or for the per-chunk `VecDeque` segment push under sustained high-throughput output."
  ]
}