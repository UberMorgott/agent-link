# Trajectory Compaction: 2026-09-01 - 2026-09-01

## Summary

Historical intermediate only; this record does not describe PR #1632 at its current head. This session replaced a negated `pgrep` cleanup check with direct, non-root `pgrep` status capture. That intermediate required both effective-UID and real-UID probes to return `pgrep` status 1 and treated statuses above 1 as errors.

A later review found that procfs `hidepid` can conceal another UID's processes from a non-root observer while still returning status 1, so the direct-probe approach was superseded. The final implementation and validation are documented in `compact_dq76p869lqu0`: `probe_builder_uid` runs `/usr/bin/pgrep` as root through `sudo --non-interactive` and `env -i` with a fixed `PATH`, maps match/no-match/error to sentinels 10/11/12, accepts only the pair `11:11`, retries only explicit match pairs, and fails closed for every wrapper or probe error. The final diagnostic `pgrep` calls also run under `sudo`.

Workflow-command parsing still resumes only after that privileged zero-process proof succeeds, and cleanup failure still takes precedence over the Cargo result. Do not use the non-root status-one design from this historical record as the current contract.

## Key Decisions (2)

| Question | Decision | Impact |
| --- | --- | --- |
| What was implemented at this historical intermediate stage? | Capture direct non-root `pgrep` exit statuses and require both to equal 1. | Superseded: under procfs `hidepid`, a non-root status 1 is not proof that another UID owns no live processes. |
| What is the current cleanup proof? | The privileged sentinel-mapped design in `compact_dq76p869lqu0`. | Root visibility prevents `hidepid` false negatives; only `11:11` proves an empty process set and every ambiguous status fails closed. |

## Conventions Established

- Treat this compact as a historical, superseded step; `compact_dq76p869lqu0` is the durable current cleanup contract.
- A security-relevant absence check must have enough privilege to observe everything it claims is absent and must fail closed on wrapper errors.

## Lessons Learned

- Fixing exit-status handling is not enough if the observer lacks complete visibility. The intermediate non-root check distinguished status 1 from statuses above 1, but `hidepid` could still produce status 1 while builder-owned processes were alive.
- Use the final root sentinel wrapper from `compact_dq76p869lqu0` and accept only the explicit no-match sentinel pair.

## Open Questions

- None.

## Stats

- Sessions: 1, Agents: default, Files: 0, Commits: 0
- Date range: 2026-09-01T21:01:49.338Z - 2026-09-01T21:02:11.300Z
