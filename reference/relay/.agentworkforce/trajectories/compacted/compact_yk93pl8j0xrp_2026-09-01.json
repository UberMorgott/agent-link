{
  "id": "compact_yk93pl8j0xrp",
  "version": 1,
  "type": "compacted",
  "compactedAt": "2026-09-01T20:41:45.983Z",
  "sourceTrajectories": [
    "traj_izlg7h9tzrw3"
  ],
  "dateRange": {
    "start": "2026-09-01T20:40:15.251Z",
    "end": "2026-09-01T20:41:45.463Z"
  },
  "summary": {
    "totalDecisions": 1,
    "totalEvents": 1,
    "uniqueAgents": [
      "default"
    ]
  },
  "decisionGroups": [
    {
      "category": "tooling",
      "decisions": [
        {
          "question": "Denied privilege gain before UID-scoped cleanup",
          "chosen": "Denied privilege gain before UID-scoped cleanup",
          "reasoning": "A dedicated UID alone does not cover setuid or file-capability transitions. Both setpriv calls now set no_new_privs and empty bounding, inheritable, and ambient capability sets; preflight proves every capability set is zero, and cleanup matches both real and effective builder UID.",
          "fromTrajectory": "traj_izlg7h9tzrw3"
        }
      ]
    }
  ],
  "keyLearnings": [],
  "keyFindings": [],
  "filesAffected": [],
  "commits": [],
  "narrative": "This session closed out the producer-side isolation design for AgentWorkforce/relay PR #1632, the PR-proof broker artifact workflow in `.github/workflows/relayflow-pr-proof-broker.yml`. The final architecture supersedes every earlier iteration — both the cache-only approach and the dedicated-UID-without-`no_new_privs` approach. The producer no longer uses `Swatinem/rust-cache` or any actions cache; the four `ACTIONS_*` environment variables and the four `GITHUB_*` command files are removed from the build step's environment, and workflow commands are suspended with randomly generated stop-command tokens so a compromised build cannot emit `::set-output`/`::add-mask`-style directives back into the runner. The broker is compiled under a freshly created dedicated build UID, and both `setpriv` invocations are hardened identically: `env -i` for a clean environment, `--clear-groups`, `--no-new-privs`, and `--bounding-set=-all --inh-caps=-all --ambient-caps=-all`. The substantive addition this session was denying privilege *gain* before UID-scoped cleanup can be trusted. A dedicated UID alone does not cover setuid binaries or file-capability transitions, so the preflight step now asserts positively rather than assuming: it reads the builder's `/proc` status and requires `CapInh`, `CapPrm`, `CapEff`, `CapBnd`, and `CapAmb` all zero and `NoNewPrivs` equal to one, failing the job otherwise. The filesystem layout backs this up — the checkout is non-writable to the build UID, the build root is runner-owned mode 0710, and only the specific build directories are delegated to the builder. Staging stays runner-owned; an `EXIT` trap fires on cargo failure so cleanup is not skipped on the error path; cleanup kills and then verifies zero remaining processes for both the *real* and *effective* builder UID; and the produced broker is copied out only after that reap completes, and only if it is a regular, non-symlink file. The resolver side — exact event, exact path, success requirement, `run.head_sha` pinning, manifest, and digest attestation — is unchanged from the prior iterations. Verification was complete rather than partial: focused PR-proof contract gates at 68 passed / 1 Linux-only skip; full Vitest at 149 files passed / 2 skipped and 2278 tests passed / 17 skipped; plus full typecheck, Prettier, `actionlint`, parsed Bash syntax, and diff check. A Debian Linux container exercised the exact `setpriv` flag set, the capability and `NoNewPrivs` assertions, a background-process kill, and the real/effective-UID zero-process check end to end.",
  "decisions": [
    {
      "question": "Is a dedicated build UID sufficient to make post-build cleanup trustworthy?",
      "chosen": "No — add `--no-new-privs` and empty bounding/inheritable/ambient capability sets to both `setpriv` calls, and assert the resulting state in preflight",
      "reasoning": "A dedicated UID scopes ownership but does not prevent a setuid binary or a file-capability transition from re-escalating mid-build, which would let a process escape UID-scoped kill/verify.",
      "impact": "Cleanup's `pkill`-then-verify by UID becomes a real guarantee rather than a best-effort sweep; preflight fails the job if any capability set is non-zero or `NoNewPrivs` is not 1."
    },
    {
      "question": "Should cleanup match processes by real UID only?",
      "chosen": "Kill and verify against both real and effective builder UID",
      "reasoning": "A process can hold a different effective UID than its real UID; matching only one leaves a class of survivors invisible to the check.",
      "impact": "The zero-remaining-processes assertion covers both, and was exercised against a real background process in the Debian container."
    },
    {
      "question": "Keep the Rust build cache for producer speed?",
      "chosen": "Drop `Swatinem/actions` caching entirely from the producer",
      "reasoning": "A restored cache is attacker-influenceable input into the trusted build; a slower cold build is the price of a provably clean producer.",
      "impact": "Supersedes the earlier cache-only iteration; producer builds are cold every run."
    },
    {
      "question": "How to prevent the untrusted build from talking back to the runner?",
      "chosen": "Strip the four `ACTIONS_*` env vars and four `GITHUB_*` command files, and suspend workflow commands with randomly generated stop-command tokens",
      "reasoning": "Removing the endpoints/handles kills the cache and command-file channels; random stop tokens close the `::` workflow-command channel against a guessed token.",
      "impact": "Build output cannot mint outputs, mask values, or reach Actions services."
    },
    {
      "question": "When is the built broker allowed to leave the sandbox?",
      "chosen": "Copy it only after cleanup completes, and only if it is a regular non-symlink file, into runner-owned staging",
      "reasoning": "Copying before the reap could race a still-live builder process; a symlink could redirect the copy outside staging.",
      "impact": "Staging never holds builder-writable content, and there is no TOCTOU window between build and publish."
    },
    {
      "question": "What happens if cargo fails partway?",
      "chosen": "Install an `EXIT` trap so cleanup runs on the failure path too",
      "reasoning": "Without the trap, a build failure leaks the delegated dirs and any surviving builder processes into subsequent steps.",
      "impact": "Cleanup is unconditional; failure and success converge on the same reaped state."
    }
  ],
  "conventions": [
    {
      "pattern": "Every privilege-drop transition uses the identical hardened invocation: `env -i` + `setpriv --clear-groups --no-new-privs --bounding-set=-all --inh-caps=-all --ambient-caps=-all`",
      "rationale": "Two `setpriv` sites with different flags means the weaker one defines the actual security boundary; keeping them byte-identical removes that asymmetry.",
      "scope": "`.github/workflows/relayflow-pr-proof-broker.yml` — preflight and cargo build steps; any future step that drops to the build UID."
    },
    {
      "pattern": "Assert the sandbox rather than assume it — preflight reads and checks `CapInh`/`CapPrm`/`CapEff`/`CapBnd`/`CapAmb` == 0 and `NoNewPrivs` == 1 before any untrusted code runs",
      "rationale": "Flags can be silently ignored by an older `setpriv` or a differently configured runner image; the assertion converts a silent no-op into a hard failure.",
      "scope": "Producer preflight; the general rule for any capability/privilege flag added to CI."
    },
    {
      "pattern": "Least-privilege filesystem layout: runner-owned 0710 build root, non-writable checkout, only the specific build dirs delegated to the builder, staging runner-owned",
      "rationale": "The builder needs write access to build outputs and nothing else; 0710 keeps the root traversable-but-opaque to the build UID.",
      "scope": "Producer job filesystem setup."
    },
    {
      "pattern": "Verification is the full gate set, not a spot check: focused contract tests, full Vitest, typecheck, Prettier, `actionlint`, parsed Bash syntax, and diff check — plus a Docker Linux smoke for anything Linux-only",
      "rationale": "Workflow YAML plus embedded Bash plus TS contract fixtures each have their own failure mode; none of the static checks would catch a `setpriv` flag that doesn't behave as documented.",
      "scope": "Any change to the PR-proof broker workflow or `tests/fixtures/pr-proof-contract.test.ts`."
    },
    {
      "pattern": "Resolver identity checks stay fixed while producer internals change: exact event, exact path, success requirement, `run.head_sha`, manifest, and digest attestation",
      "rationale": "The consumer's trust decision must not drift as the producer is re-architected; holding it constant makes each producer iteration independently reviewable.",
      "scope": "PR-proof artifact resolution."
    }
  ],
  "lessons": [
    {
      "lesson": "UID isolation and privilege-gain denial are separate controls; shipping the first without the second yields a sandbox that looks scoped but isn't",
      "context": "The prior iteration used a dedicated build UID but no `no_new_privs` and no capability clearing, leaving setuid/file-capability escalation paths open beneath a cleanup routine that assumed UID scoping held.",
      "recommendation": "Whenever CI drops to a lower-privileged UID, pair it with `--no-new-privs` and emptied bounding/inheritable/ambient caps in the same invocation, and prove it in preflight."
    },
    {
      "lesson": "Linux-only sandbox semantics cannot be validated by the repo's test suite — the focused contract run legitimately skips them",
      "context": "The focused gate is 68 passed / 1 Linux skip; that skipped behavior is exactly the `setpriv`/capability path that matters most.",
      "recommendation": "Back Linux-only CI hardening with an explicit Debian container smoke covering the exact flags, the capability/`NoNewPrivs` assertions, background-process kill, and the real/effective-UID zero-process check — as was done here."
    },
    {
      "lesson": "Convenience inputs to a trusted build (caches, Actions env vars, command files) are attack surface, and removing them is cheaper than reasoning about them",
      "context": "Dropping `Swatinem/actions` caching, four `ACTIONS_*` vars, and four `GITHUB_*` command files, then randomizing stop-command tokens, eliminated whole channels instead of filtering them.",
      "recommendation": "In artifact producers, prefer deleting a channel over sanitizing it; accept the cold-build cost."
    },
    {
      "lesson": "Cleanup must be reachable on the error path and must precede any artifact copy-out",
      "context": "An `EXIT` trap was added for cargo failure, and the regular non-symlink broker copy was ordered strictly after the reap.",
      "recommendation": "Order sandboxed builds as build → trap-protected reap → verify zero processes (real and effective UID) → type-check the file → copy to runner-owned staging."
    }
  ],
  "openQuestions": [
    "The Linux-only sandbox assertions remain outside the repo's automated suite (1 focused skip / 17 full-suite skips) and were proven only by a manual Debian container run; whether that container smoke should be wired into CI as a recurring job is undecided."
  ]
}