{
  "id": "compact_osj9nwnr5ucy",
  "version": 1,
  "type": "compacted",
  "compactedAt": "2026-09-02T05:48:29.781Z",
  "sourceTrajectories": [
    "traj_gztbvhtaqbv9"
  ],
  "dateRange": {
    "start": "2026-09-02T05:29:08.070Z",
    "end": "2026-09-02T05:48:29.218Z"
  },
  "summary": {
    "totalDecisions": 1,
    "totalEvents": 2,
    "uniqueAgents": [
      "default"
    ]
  },
  "decisionGroups": [
    {
      "category": "other",
      "decisions": [
        {
          "question": "Serialize completed PTY reads with verified write admission at the read/tag boundary",
          "chosen": "Serialize completed PTY reads with verified write admission at the read/tag boundary",
          "reasoning": "Unix readiness polling outside the mutex keeps idle prompt writes immediate, while ready read plus sequence assignment inside the mutex closes the read-return/pre-tag race. Windows ConPTY exposes only blocking ReadFile, so verified admission repeatedly cancels that synchronous read until it acquires the same mutex or fails closed at a bounded deadline. Parser work and bounded output publication remain outside the mutex to preserve output order without backpressure deadlock.",
          "fromTrajectory": "traj_gztbvhtaqbv9"
        }
      ]
    }
  ],
  "keyLearnings": [],
  "keyFindings": [],
  "filesAffected": [
    "Cargo.lock",
    "crates/relay-pty/Cargo.toml",
    "crates/relay-pty/src/pty.rs"
  ],
  "commits": [
    "a7bdbd672"
  ],
  "narrative": "Single 19-minute session (PR #1634, commit a7bdbd672 `fix(pty): linearize reads with verified writes`) closing a residual race in the verified-write/stale-echo machinery that the preceding broker commits (99d4836a6 `reject queued stale PTY echoes`, e9bc2666d Claude wrapper support) depend on. Before this change, `crates/relay-pty/src/pty.rs` published a chunk's producer sequence *after* `reader.read()` returned, taking `output_order` only around the `output_sequence.fetch_add`. That left a window where bytes had already been copied out of the PTY but were not yet tagged: a concurrent `enqueue_write_with_output_boundary` could grab `output_order`, sample `output_sequence` as the boundary, and thereby classify already-in-flight (stale) output as *post-write* echo — defeating the broker's echo rejection. The fix moves the read itself inside the critical section via a new helper `read_and_tag_output(reader, buf, output_order, output_sequence, after_read)`, which locks `output_order`, performs `reader.read`, invokes an injected `after_read` callback (no-op in production, a test rendezvous point in tests), then assigns the sequence — all before releasing the lock. To avoid the obvious consequence (an idle reader blocking inside `read` would hold the lock forever and stall prompt injection), the session splits by platform: on Unix a new `wait_for_pty_readable(&OwnedFd)` does a `libc::poll(POLLIN, -1)` on a `dup`'d master fd *outside* the lock, so only a ready read is taken under it; on Windows, where ConPTY exposes only a blocking `ReadFile`, `enqueue_write_with_output_boundary` spins on `output_order.try_lock()` and calls `ReaderCancel::interrupt_read()` (`CancelSynchronousIo` on a `DuplicateHandle`'d reader-thread handle, treating `ERROR_NOT_FOUND` as benign) until it acquires the lock or hits a bounded 1s `OUTPUT_ORDERING_TIMEOUT` and fails closed with the new `PtyWriteSubmitError::OrderingTimeout` / `OrderingWake` variants. The reader treats `ERROR_OPERATION_ABORTED` (`read_was_cancelled`) as `continue`, not EOF. Alacritty parsing (`processor.advance`) and the bounded `tx.blocking_send(PtyOutput { .. })` deliberately stay *outside* `output_order` so consumer backpressure can never block write admission. Two deterministic regression tests were added rather than timing-dependent ones: `completed_read_is_tagged_before_verified_write_can_sample_boundary` uses the `after_read` hook plus std channels to freeze the reader exactly in the old read-complete/pre-tag gap and asserts the submitter cannot sample a boundary there (`stale_sequence <= boundary`, and a subsequent fresh read gets `fresh_sequence > boundary`); `output_channel_backpressure_does_not_hold_verified_write_ordering` fills a `mpsc::channel(1)` and asserts boundary acquisition still completes in <100ms. The existing live-PTY test also gained a `submitted_at.elapsed() < 1s` assertion proving admission isn't serialized behind an idle read. Reported verification: relay-pty 228/228, broker 1037/1037 active, fmt/clippy/diff clean, Windows relay-pty cross-clippy green. New dependency: `windows-sys 0.59` under `[target.'cfg(windows)'.dependencies]`. Retro confidence implied high; work landed on branch `fix/claude-injection-submit-0902`.",
  "decisions": [
    {
      "question": "Where should the boundary between 'stale' and 'post-write' PTY output be established?",
      "chosen": "At the read/tag boundary — `read_and_tag_output` holds `output_order` across `reader.read()` *and* the `output_sequence.fetch_add`, instead of locking only around the tag.",
      "reasoning": "Publishing the sequence after `read()` returned left a gap in which copied-but-untagged bytes were invisible to a submitter sampling the watermark, so pre-existing output could be misclassified as echo of the new write.",
      "impact": "Verified-write admission and stale-echo rejection in the broker are now sound; the guarantee is `stale_sequence <= boundary < fresh_sequence` by construction."
    },
    {
      "question": "How to keep an idle reader blocked in `read` from holding the ordering lock and stalling prompt injection?",
      "chosen": "Unix: `libc::poll(POLLIN)` readiness wait on a `dup`'d master fd outside the lock (`wait_for_pty_readable`). Windows: submitter spins `try_lock` + `CancelSynchronousIo` on a duplicated reader-thread handle, bounded by a 1s deadline.",
      "reasoning": "Unix can cheaply prove readiness before entering the critical section; ConPTY offers only a synchronous blocking `ReadFile`, so the only way in is to interrupt the reader. A bounded deadline avoids an unbounded stall.",
      "impact": "Idle-prompt writes stay immediate on Unix; on Windows they fail closed (retryable `OrderingTimeout`/`OrderingWake`) rather than hanging. Adds `windows-sys` and unsafe Win32 handle code confined to `ReaderCancel`."
    },
    {
      "question": "What stays inside the ordering critical section?",
      "chosen": "Only readiness-confirmed `read` + sequence assignment. Alacritty `processor.advance`/`term` locking, `consumed_offset` publication, and the bounded `blocking_send` of `PtyOutput` remain outside.",
      "reasoning": "The bounded output channel (`WRITE_QUEUE_DEPTH`-style backpressure) can block indefinitely on a slow consumer; holding `output_order` there would deadlock write admission.",
      "impact": "Output ordering is preserved without coupling admission latency to consumer drain rate; asserted by `output_channel_backpressure_does_not_hold_verified_write_ordering`."
    },
    {
      "question": "How to test a read-complete/pre-tag interleaving that is otherwise unobservable?",
      "chosen": "Inject an `after_read: FnOnce(usize)` callback into `read_and_tag_output` — a no-op in production, a channel rendezvous in tests — and drive it with `Cursor`/`ChunkReader` fakes plus std sync channels.",
      "reasoning": "Sleep- or load-based reproduction of the race is nondeterministic; a seam inside the critical section makes the exact interleaving reproducible on every run.",
      "impact": "Regressions are caught deterministically instead of flakily; the seam costs one monomorphized no-op closure in production."
    },
    {
      "question": "Should the synchronous `relay-pty`/broker APIs change to accommodate the fix?",
      "chosen": "No — keep `PtySession::submit_write*` synchronous; confine all new behavior to a narrow platform-specific critical section.",
      "reasoning": "Turning admission async would ripple through the broker's call sites for a fix that is fundamentally about lock scope.",
      "impact": "Zero blast radius outside `crates/relay-pty/src/pty.rs` (plus Cargo.toml/lock); broker suite passed unchanged."
    }
  ],
  "conventions": [
    {
      "pattern": "Sequence/watermark assignment must happen in the same critical section as the data acquisition it describes — never `acquire; unlock; tag`.",
      "rationale": "Any gap between copying bytes and stamping their identity is a window where a concurrent sampler reads a boundary that lies about what already exists.",
      "scope": "Any producer/consumer ordering watermark in the broker and relay-pty (`output_sequence`, `consumed_offset`, echo/boundary logic)."
    },
    {
      "pattern": "Keep blocking or bounded-channel work (parsing, `blocking_send`) strictly outside ordering locks; hold locks only across non-blocking, readiness-proven operations.",
      "rationale": "Backpressure inside an ordering lock converts a slow consumer into a producer-side deadlock.",
      "scope": "crates/relay-pty, crates/broker."
    },
    {
      "pattern": "Expose a test-only seam as an injected callback parameter (`after_read: F`) on the function under test rather than reproducing races with sleeps or thread counts.",
      "rationale": "Deterministic interleaving tests actually bite; timing tests rot into flakes and get ignored.",
      "scope": "Concurrency fixes in Rust crates in this repo."
    },
    {
      "pattern": "Platform divergence is expressed with `#[cfg(unix)]`/`#[cfg(windows)]` inside one function plus Windows-only deps under `[target.'cfg(windows)'.dependencies]`, and Windows-only error variants added to the existing `thiserror` enum (`PtyWriteSubmitError::OrderingTimeout`, `OrderingWake`) — not a second code path or crate.",
      "rationale": "Matches `.claude/rules/rust.md` (thiserror for matched library errors, target-gated deps, minimize dependencies) and keeps the two platforms' invariants visibly aligned.",
      "scope": "crates/relay-pty, crates/broker."
    },
    {
      "pattern": "Unbounded blocking against a foreign platform primitive gets a deadline and a retryable, explicitly-named error rather than an infinite wait.",
      "rationale": "`CancelSynchronousIo` can race the reader between syscalls; failing closed at 1s (`OUTPUT_ORDERING_TIMEOUT`) with a caller-retryable error keeps the session responsive.",
      "scope": "Windows paths in relay-pty."
    },
    {
      "pattern": "Cross-platform Rust changes are validated with Windows cross-clippy/tests even when developing on macOS, and the retro cites exact suite counts (228/228, 1037/1037) rather than 'tests pass'.",
      "rationale": "Half of this fix compiles only under `cfg(windows)`; an unverified branch is an unwritten branch.",
      "scope": "All crates/ changes."
    }
  ],
  "lessons": [
    {
      "lesson": "A lock placed around the *publication* of an ordering token does not order the token against the *acquisition* it names.",
      "context": "The pre-fix reader locked `output_order` only around `fetch_add`; bytes copied by `reader.read()` before the lock were invisible to a submitter that had already sampled the watermark, so the broker's stale-echo rejection could be defeated by exactly the interleaving it was written to prevent.",
      "recommendation": "When reviewing a watermark/sequence scheme, ask what state exists between the syscall and the tag, and pull the syscall inside the lock (adding a readiness wait outside it) rather than widening the sampler's tolerance."
    },
    {
      "lesson": "Moving a blocking read inside a lock is only safe if you have a way to not block there — and that mechanism differs per platform.",
      "context": "Unix could poll readiness outside the lock; Windows ConPTY's blocking `ReadFile` forced the inverse design where the *writer* cancels the reader's syscall via a duplicated thread handle.",
      "recommendation": "Before adopting a 'read under the lock' design, confirm each target platform offers either readiness polling or read cancellation; budget for a bounded fail-closed path where neither is clean."
    },
    {
      "lesson": "Races that were previously only reproducible under load became cheap regression tests once a callback seam existed inside the critical section.",
      "context": "`completed_read_is_tagged_before_verified_write_can_sample_boundary` freezes the reader in the exact former gap; `output_channel_backpressure_does_not_hold_verified_write_ordering` proves the lock-scope invariant with a `channel(1)`.",
      "recommendation": "Add the seam as part of the fix, not afterwards — and assert the *negative* (submitter must NOT proceed within 50ms) as well as the positive ordering."
    },
    {
      "lesson": "Fixing the reader side changed an unrelated code path's cfg surface (`no_pid_exit_threshold` became `#[cfg(unix)]`, and `resolve_command_path` imports moved under `#[cfg(unix)]` in tests).",
      "context": "Windows cross-clippy surfaced dead-code/unused-import warnings once the Unix-only branches were split out.",
      "recommendation": "Run the Windows clippy pass before declaring a platform-split change done; expect to gate incidental helpers, not just the new code."
    }
  ],
  "openQuestions": [
    "Is 1s (`OUTPUT_ORDERING_TIMEOUT`) the right Windows deadline, and does any caller actually retry on `PtyWriteSubmitError::OrderingTimeout`/`OrderingWake`, or does the broker surface it as a hard injection failure?",
    "The Windows admission path is a busy spin (`try_lock` + `CancelSynchronousIo` + `thread::yield_now`) with no backoff — what is its CPU cost under a chatty child that keeps the reader re-entering `ReadFile`?",
    "Windows behavior was verified only by cross-clippy plus relay-pty tests compiled for Windows; there is no report of the fix exercised against a real ConPTY session on Windows hardware.",
    "The extra `libc::dup` of the PTY master for readiness polling adds an fd per session — has fd lifetime/teardown (shutdown ordering vs. the detached reader thread) been checked at fleet scale?",
    "PR #1634's merge/review status and whether the companion broker change (99d4836a6) ships in the same release are not recorded in the trajectory."
  ]
}