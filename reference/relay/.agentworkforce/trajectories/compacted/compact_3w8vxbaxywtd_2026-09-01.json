{
  "id": "compact_3w8vxbaxywtd",
  "version": 1,
  "type": "compacted",
  "compactedAt": "2026-09-01T20:54:26.278Z",
  "sourceTrajectories": [
    "traj_ehg23rsfycm6"
  ],
  "dateRange": {
    "start": "2026-09-01T20:54:11.028Z",
    "end": "2026-09-01T20:54:25.801Z"
  },
  "summary": {
    "totalDecisions": 1,
    "totalEvents": 1,
    "uniqueAgents": [
      "default"
    ]
  },
  "decisionGroups": [
    {
      "category": "api",
      "decisions": [
        {
          "question": "Return reap failure before Cargo failure",
          "chosen": "Return reap failure before Cargo failure",
          "reasoning": "The explicit post-build path must match the EXIT trap: if both Cargo and UID reaping fail, the unproven security boundary is the terminal error and workflow commands remain suspended. The Cargo status is returned only after cleanup succeeds.",
          "fromTrajectory": "traj_ehg23rsfycm6"
        }
      ]
    }
  ],
  "keyLearnings": [],
  "keyFindings": [],
  "filesAffected": [],
  "commits": [],
  "narrative": "This session is the terminal correction in the PR #1632 line of work (extending compacted trajectory compact_yk93pl8j0xrp), which hardens the PR-proof broker build in `.github/workflows/relayflow-pr-proof-broker.yml` against PR-authored code escaping its sandbox. The single substantive change was ordering the failure semantics of the post-build cleanup so that the security boundary — reaping every process owned by the real and effective builder UID — always outranks the build result. Concretely: workflow-command parsing (the `stop-commands` / `echo \"::stop-commands::\"` suspension that prevents PR-authored build output from injecting `::set-output`/`::add-mask` directives into the runner) is resumed on both exit paths — the `trap ... EXIT` handler and the explicit inline post-build path — only after the zero-process reap check succeeds. If the reap fails, parsing stays suspended and the job terminates in that state; that is the intended terminal condition, not a bug to be papered over. The precedence chain was pinned in exactly one order and asserted structurally: cleanup runs first, then `KILL_STATUS` failure is surfaced, then `BUILD_STATUS` failure, and only then does the job proceed to trusted staging of artifacts. The consequence is that a job in which both Cargo failed and UID reaping failed reports the reap failure — the unproven security boundary — as the terminal error; the Cargo status is preserved and returned only after cleanup has succeeded. Because this ordering is a property of the shell source rather than of any single observable behavior, the contract is enforced by a source-ordering assertion in `tests/fixtures/pr-proof-contract.test.ts` that reads the workflow YAML and checks the four markers appear in sequence, with the rationale written up in `tests/relayflows/cases/README.md` so future editors don't reshuffle the block for readability. Gates run on the final state: the focused PR-proof contract suite at 68 passing with 1 Linux-only skip (the reap assertion that requires Linux `/proc` semantics and cannot execute on the darwin dev host), plus Prettier, actionlint, a parsed Bash syntax check of the embedded `run:` blocks, and a diff check. No files or commits were produced in this session window itself — the work is the final semantic correction landing on branch `ci/pr-proof-broker-artifacts-0901`, whose recent history (`fe20fae2d fix(ci): isolate PR-authored broker builds`, `a8e4df123 fix(ci): bound concurrent broker resolution`) carries the surrounding hardening. Nothing was left unresolved.",
  "decisions": [
    {
      "question": "When both the Cargo build and the builder-UID process reap fail, which failure does the job report?",
      "chosen": "Return the reap failure before the Cargo failure; surface `KILL_STATUS` first, `BUILD_STATUS` second.",
      "reasoning": "A failed reap means the security boundary was never proven — PR-authored processes may still be alive holding the runner. That is strictly more severe than a build that merely didn't compile, and it must not be masked by the louder, more expected Cargo error. Cargo status is meaningful only once the sandbox is known clean, so it is preserved and returned after cleanup succeeds.",
      "impact": "Deterministic terminal error for the both-failed case; the explicit post-build path now matches the EXIT trap exactly, so the two exit routes cannot diverge in what they report or in whether they resume command parsing."
    },
    {
      "question": "Should workflow-command parsing be restored on the failure path so downstream steps can annotate?",
      "chosen": "No — resume parsing only after a successful zero-process reap; a reap failure leaves the parser suspended and is terminal.",
      "reasoning": "Restoring `::` command parsing while PR-authored processes may still be running would reopen the exact injection surface the suspension exists to close. Fail-closed is the only safe default at that boundary.",
      "impact": "A reap failure produces a job that ends with commands still suspended — intentionally degraded annotation output in exchange for no injection window."
    },
    {
      "question": "How do you test an ordering property that has no distinguishing runtime observable on the dev host?",
      "chosen": "A source-ordering contract assertion in `tests/fixtures/pr-proof-contract.test.ts` over the workflow YAML, checking cleanup → KILL_STATUS failure → BUILD_STATUS failure → trusted staging, with the Linux-only reap behavior test skipped on darwin.",
      "reasoning": "The precedence is a property of statement order in the embedded shell; asserting it against the source catches a reordering refactor that no darwin-runnable behavioral test could see.",
      "impact": "68 pass / 1 Linux skip. The skipped case is a known, documented coverage gap on macOS, not a silent hole."
    }
  ],
  "conventions": [
    {
      "pattern": "Both exit routes out of the broker build — the `trap ... EXIT` handler and the explicit inline post-build path — must be semantically identical: same cleanup, same precedence, same resume-parsing condition.",
      "rationale": "Divergence between a trap and its inline twin is a classic latent hole: the path exercised in CI is not the path exercised on the failure you care about.",
      "scope": ".github/workflows/relayflow-pr-proof-broker.yml, and any future workflow embedding a sandboxed PR-authored build."
    },
    {
      "pattern": "Fail-closed ordering: run cleanup first, surface cleanup failure before build failure, and only reach trusted staging after both are clean. Never restore workflow-command parsing before the security check passes.",
      "rationale": "Security-boundary status outranks functional status; staging artifacts is a trusted operation that must be unreachable from an unproven sandbox.",
      "scope": "PR-proof broker CI and any job that stages artifacts produced by untrusted code."
    },
    {
      "pattern": "Encode order-dependent shell semantics as a source-ordering assertion in the contract test plus a README rationale, rather than relying on a behavioral test alone.",
      "rationale": "Ordering has no observable difference until the rare both-failed case; without a source assertion a well-meaning readability refactor silently inverts the precedence.",
      "scope": "tests/fixtures/pr-proof-contract.test.ts, tests/relayflows/cases/README.md."
    },
    {
      "pattern": "Gate the workflow on the full set — focused contract suite, Prettier, actionlint, parsed Bash syntax of embedded `run:` blocks, and a diff check — not just the test run.",
      "rationale": "actionlint and a real Bash parse catch YAML/shell breakage that a TypeScript contract test reading the file as text will happily pass.",
      "scope": "Any change to .github/workflows/relayflow-pr-proof-broker.yml."
    }
  ],
  "lessons": [
    {
      "lesson": "When two independent failures can occur in one exit path, the precedence must be chosen deliberately and asserted — not left to whichever `exit` happens to run first.",
      "context": "Cargo failure and UID-reap failure can co-occur; the naive ordering would report the build error and hide the fact that the sandbox was never proven clean.",
      "recommendation": "For any cleanup-plus-work pair, state the precedence explicitly in the source and pin it with a test that reads the source order."
    },
    {
      "lesson": "A degraded-but-safe terminal state is an acceptable, intentional outcome and should be documented as such so it isn't 'fixed' later.",
      "context": "Reap failure leaves workflow-command parsing suspended, which looks like a bug (missing annotations) to anyone reading only the job log.",
      "recommendation": "Record the intent in the README next to the contract test so the suspended-parser end state reads as designed, not broken."
    },
    {
      "lesson": "A platform-skipped test is a real coverage gap and must be reported as such, not folded into a clean pass count.",
      "context": "1 of 69 contract cases is Linux-only (builder-UID reap semantics) and skips on the darwin dev host.",
      "recommendation": "Always state gates as '68 pass / 1 Linux skip' rather than 'all green'; confirm the skipped case executes on the Linux runner in CI."
    }
  ],
  "openQuestions": []
}